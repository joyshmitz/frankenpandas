# br-frankenpandas-fi6zx: REJECTED lever — fused boolean-mask column filter
# 2026-06-06, cc/BrightOtter. filter_bool 100000x10 (50% alternating mask),
# internal per-iter timer, interleaved before/after pairs.
#
# Variant 1 (branchy push): load ~60
#   before: 1.627 / 1.558 / 1.640 ms/iter
#   after:  2.013 / 1.781 / 2.763 ms/iter   -> after SLOWER in 3/3 pairs
# Variant 2 (branchless to_vec + in-place store-and-conditionally-advance): load ~23
#   before: 1.050 / 1.003 / 1.050 / 0.984 ms/iter
#   after:  1.344 / 1.230 / 1.238 / 1.171 ms/iter -> after SLOWER ~20% in 4/4 pairs
#
# Why it loses: existing positions+gather path costs one 100k positions build
# amortized over 10 columns + 10x 50k typed gathers (~600k touches); both fused
# variants touch all 100k slots per column (1M+), and the branchless variant
# adds a full memcpy (~2.4x memory traffic). The positions path is already
# memory-optimal at this density.
#
# Context: the epic's 11.5x filter_bool gap (2026-06-02 baseline) is ALREADY
# CLOSED by landed typed-gather work: FP 1.146ms vs pandas 2.2.3 1.236ms
# (same box, same load) = FP 1.08x FASTER. Goldens during trial stayed
# byte-identical (eeeba630/2f77640d/0d34cbf3/64d27a6b before == after).
# Source hunks removed; only the clippy hygiene allow on the 227ec932 rank
# loop is retained.
