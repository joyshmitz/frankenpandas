Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-conformance/examples/bench_csv_write.rs:7:
 //!
 //! Run: cargo run -p fp-conformance --example bench_csv_write --release
 
-use std::collections::BTreeMap;
-use std::time::Instant;
+use std::{collections::BTreeMap, time::Instant};
 
 use fp_columnar::Column;
 use fp_frame::DataFrame;
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-conformance/examples/bench_csv_write.rs:38:
         order.push(name);
     }
     for c in 0..2 {
-        let v: Vec<i64> = (0..n as i64).map(|i| i.wrapping_mul(31 + c) % 9973).collect();
+        let v: Vec<i64> = (0..n as i64)
+            .map(|i| i.wrapping_mul(31 + c) % 9973)
+            .collect();
         let name = format!("i{c}");
         cols.insert(name.clone(), Column::from_i64_values(v));
         order.push(name);
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-conformance/examples/bench_csv_write.rs:53:
         bytes.extend_from_slice(samples[i % samples.len()].as_bytes());
         offsets.push(bytes.len());
     }
-    cols.insert("s".to_string(), Column::from_utf8_contiguous(bytes, offsets));
+    cols.insert(
+        "s".to_string(),
+        Column::from_utf8_contiguous(bytes, offsets),
+    );
     order.push("s".to_string());
     DataFrame::new_with_column_order(index, cols, order).expect("frame")
 }
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-conformance/examples/bench_csv_write.rs:73:
     let s = write_csv_string(&small).expect("csv");
     println!("SMALL_FNV {:016x} len={}", fnv1a64(&s), s.len());
     // show the first line's terminator bytes
-    let head: String = s.chars().take(40).map(|c| if c == '\n' { '|' } else if c == '\r' { '^' } else { c }).collect();
+    let head: String = s
+        .chars()
+        .take(40)
+        .map(|c| {
+            if c == '\n' {
+                '|'
+            } else if c == '\r' {
+                '^'
+            } else {
+                c
+            }
+        })
+        .collect();
     println!("HEAD {head}");
 
     let frame = build(n);
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-conformance/examples/perf_profile.rs:178:
 fn build_groupby_cum_pair(n: usize, num_groups: usize) -> (Series, Series) {
     let labels: Vec<IndexLabel> = (0..n).map(|i| IndexLabel::Int64(i as i64)).collect();
     let keys: Vec<i64> = (0..n).map(|i| (i % num_groups) as i64).collect();
-    let vals: Vec<f64> = (0..n).map(|i| (i.wrapping_mul(37) % 9973) as f64 * 0.25).collect();
+    let vals: Vec<f64> = (0..n)
+        .map(|i| (i.wrapping_mul(37) % 9973) as f64 * 0.25)
+        .collect();
     let value = Series::new(
         "v".to_string(),
         Index::new(labels.clone()),
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-conformance/examples/perf_profile.rs:609:
     let mut names: Vec<String> = Vec::with_capacity(ncols + 1);
     names.push("id".to_string());
     let mut cols: Vec<(String, Vec<Scalar>)> = Vec::with_capacity(ncols + 1);
-    let keys: Vec<Scalar> = (0..n as i64).map(|row| Scalar::Int64(row + key_offset)).collect();
+    let keys: Vec<Scalar> = (0..n as i64)
+        .map(|row| Scalar::Int64(row + key_offset))
+        .collect();
     cols.push(("id".to_string(), keys));
     for c in 0..ncols {
         let name = format!("{value_prefix}{c}");
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-conformance/examples/perf_profile.rs:723:
             .expect("cumsum"),
         "groupby_diff" => {
             let (value, key) = build_groupby_cum_pair(n, 100);
-            let out = value
-                .groupby(&key)
-                .expect("groupby")
-                .diff(1)
-                .expect("diff");
+            let out = value.groupby(&key).expect("groupby").diff(1).expect("diff");
             return print!("{}", golden_dump_series(&out));
         }
         "df_groupby_diff" => build_transform_frame(n, 100, 4)
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-conformance/examples/perf_profile.rs:1010:
             let mut idx: Vec<i64> = (0..n as i64).collect();
             let mut state: u64 = 0x243F_6A88_85A3_08D3;
             for i in (1..n).rev() {
-                state = state.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
+                state = state
+                    .wrapping_mul(6364136223846793005)
+                    .wrapping_add(1442695040888963407);
                 let j = (state >> 33) as usize % (i + 1);
                 idx.swap(i, j);
             }
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-conformance/examples/perf_profile.rs:1229:
         "groupby_diff" => {
             let (value, key) = build_groupby_cum_pair(n, 100);
             for _ in 0..iters {
-                let out = value
-                    .groupby(&key)
-                    .expect("groupby")
-                    .diff(1)
-                    .expect("diff");
+                let out = value.groupby(&key).expect("groupby").diff(1).expect("diff");
                 sink = sink.wrapping_add(out.len());
             }
         }
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-conformance/examples/perf_profile.rs:1645:
             let mut idx: Vec<i64> = (0..n as i64).collect();
             let mut state: u64 = 0x243F_6A88_85A3_08D3;
             for i in (1..n).rev() {
-                state = state.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
+                state = state
+                    .wrapping_mul(6364136223846793005)
+                    .wrapping_add(1442695040888963407);
                 let j = (state >> 33) as usize % (i + 1);
                 idx.swap(i, j);
             }
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_dropna.rs:12:
 //! GATHER-bound: the cost is filter_rows building the kept-row output frame
 //! (take_positions per column, already typed), not the missing detection.
 
-use std::collections::BTreeMap;
-use std::hint::black_box;
-use std::time::Instant;
+use std::{collections::BTreeMap, hint::black_box, time::Instant};
 
 use fp_columnar::Column;
 use fp_frame::DataFrame;
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_dropna.rs:26:
     let mut order = Vec::new();
     for c in 0..6 {
         let name = format!("i{c}");
-        map.insert(name.clone(), Column::from_i64_values((0..n as i64).collect()));
+        map.insert(
+            name.clone(),
+            Column::from_i64_values((0..n as i64).collect()),
+        );
         order.push(name);
     }
     let name = "s".to_string();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_dropna.rs:54:
 
 fn main() {
     let args: Vec<String> = std::env::args().collect();
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let iters: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(30);
     let df = build(n);
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_groupby_shift.rs:8:
 //! digest of the full output (incl. group-boundary nulls) so the two runs prove
 //! bit-identicality.
 
-use std::hint::black_box;
-use std::time::Instant;
+use std::{hint::black_box, time::Instant};
 
 use fp_columnar::Column;
 use fp_frame::Series;
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_groupby_shift.rs:18:
 
 fn main() {
     let args: Vec<String> = std::env::args().collect();
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let iters: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(30);
     let groups = 100usize;
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_groupby_shift.rs:25:
     let labels: Vec<IndexLabel> = (0..n as i64).map(IndexLabel::Int64).collect();
     let keys: Vec<i64> = (0..n).map(|i| (i % groups) as i64).collect();
-    let vals: Vec<f64> = (0..n).map(|i| (i.wrapping_mul(37) % 9973) as f64 * 0.25).collect();
+    let vals: Vec<f64> = (0..n)
+        .map(|i| (i.wrapping_mul(37) % 9973) as f64 * 0.25)
+        .collect();
     let value = Series::new(
         "v".to_string(),
         Index::new(labels.clone()),
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_rolling_var.rs:114:
                     f64::NAN
                 } else {
                     let mean = nums.iter().sum::<f64>() / nums.len() as f64;
-                    (nums.iter().map(|x| (x - mean).powi(2)).sum::<f64>()
-                        / (nums.len() - 1) as f64)
+                    (nums.iter().map(|x| (x - mean).powi(2)).sum::<f64>() / (nums.len() - 1) as f64)
                         .sqrt()
                 }
             }
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_rolling_var.rs:204:
             for (g, w) in got_vals.iter().zip(want.iter()) {
                 let matches = match g {
                     Scalar::Null(_) => w.is_nan() && mp_eff > 0, // min_periods-null sentinel
-                    Scalar::Float64(f) => {
-                        f.to_bits() == w.to_bits() || (f.is_nan() && w.is_nan())
-                    }
+                    Scalar::Float64(f) => f.to_bits() == w.to_bits() || (f.is_nan() && w.is_nan()),
                     _ => false,
                 };
                 if !matches {
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_rolling_var.rs:220:
         } else {
             bad += 1;
             if bad <= 3 {
-                eprintln!("MISMATCH n={n} w={window} mp={mp:?} c={center} agg={}", agg_name(a));
+                eprintln!(
+                    "MISMATCH n={n} w={window} mp={mp:?} c={center} agg={}",
+                    agg_name(a)
+                );
             }
         }
     }
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_to_numpy.rs:5:
 //!
 //! Run: cargo run -p fp-frame --example bench_to_numpy --release
 
-use std::collections::BTreeMap;
-use std::hint::black_box;
-use std::time::Instant;
+use std::{collections::BTreeMap, hint::black_box, time::Instant};
 
 use fp_columnar::Column;
 use fp_frame::DataFrame;
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_to_numpy.rs:31:
     let mut cols = BTreeMap::new();
     let mut order = Vec::new();
     for c in 0..6 {
-        let v: Vec<f64> = (0..n).map(|i| ((i as u64).wrapping_mul(2654435761 + c) % 100000) as f64 * 0.125).collect();
+        let v: Vec<f64> = (0..n)
+            .map(|i| ((i as u64).wrapping_mul(2654435761 + c) % 100000) as f64 * 0.125)
+            .collect();
         let name = format!("f{c}");
         cols.insert(name.clone(), Column::from_f64_values(v));
         order.push(name);
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_to_numpy.rs:38:
     }
     for c in 0..2 {
-        let v: Vec<i64> = (0..n as i64).map(|i| i.wrapping_mul(31 + c) % 9973).collect();
+        let v: Vec<i64> = (0..n as i64)
+            .map(|i| i.wrapping_mul(31 + c) % 9973)
+            .collect();
         let name = format!("i{c}");
         cols.insert(name.clone(), Column::from_i64_values(v));
         order.push(name);
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_to_numpy.rs:46:
 }
 
 fn main() {
-    let n: usize = std::env::args().nth(1).and_then(|s| s.parse().ok()).unwrap_or(500_000);
-    let iters: usize = std::env::args().nth(2).and_then(|s| s.parse().ok()).unwrap_or(20);
+    let n: usize = std::env::args()
+        .nth(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(500_000);
+    let iters: usize = std::env::args()
+        .nth(2)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(20);
 
     let small = build(37);
     println!("GOLDEN_FNV {:016x}", fnv_f64(&small.to_numpy()));
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/bench_to_numpy.rs:61:
         sink = sink.wrapping_add(m.len());
     }
     let d = t.elapsed();
-    println!("TIMING n={n} iters={iters} per_iter={:.3}ms sink={sink}", d.as_secs_f64() * 1e3 / iters as f64);
+    println!(
+        "TIMING n={n} iters={iters} per_iter={:.3}ms sink={sink}",
+        d.as_secs_f64() * 1e3 / iters as f64
+    );
 }
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/examples/corr_parity_dump.rs:5:
 //!
 //! Run: cargo run -p fp-frame --example corr_parity_dump --release
 
-use std::io::Write;
+use std::{collections::BTreeMap, io::Write};
 
-use fp_frame::DataFrame;
 use fp_columnar::Column;
+use fp_frame::DataFrame;
 use fp_index::{Index, IndexLabel};
 use fp_types::Scalar;
-use std::collections::BTreeMap;
 
 fn splitmix_unit(i: usize, c: usize) -> f64 {
     // Identical to perf_profile::build_corr_frame.
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:4813:
         // sources), and out-of-range / absent targets map to `None` exactly as
         // `HashMap::get` returns `None`.
         let positions: Vec<Option<usize>> =
-            reindex_positions_int64_direct(self.index.labels(), new_index.labels())
-                .unwrap_or_else(|| {
+            reindex_positions_int64_direct(self.index.labels(), new_index.labels()).unwrap_or_else(
+                || {
                     let current_map = self.index.position_map_first();
                     new_index
                         .labels()
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:4821:
                         .iter()
                         .map(|label| current_map.get(label).copied())
                         .collect()
-                });
+                },
+            );
 
         let col = self.column.reindex_by_positions(&positions)?;
         Self::new(self.name.clone(), new_index, col)
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:12608:
             let mut flags = vec![false; nrows];
             #[allow(clippy::needless_range_loop)] // row indexes the offsets pairs
             for i in 0..nrows {
-                if seen.insert(&bytes[offsets[i]..offsets[i + 1]], ()).is_some() {
+                if seen
+                    .insert(&bytes[offsets[i]..offsets[i + 1]], ())
+                    .is_some()
+                {
                     flags[i] = true;
                 }
             }
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:12661:
             let flags: Vec<bool> = match keep {
                 DuplicateKeep::First => {
                     let mut seen: FxHashMap<&[u8], ()> = FxHashMap::default();
-                    (0..nrows).map(|i| seen.insert(span(i), ()).is_some()).collect()
+                    (0..nrows)
+                        .map(|i| seen.insert(span(i), ()).is_some())
+                        .collect()
                 }
                 DuplicateKeep::Last => {
                     let mut seen: FxHashMap<&[u8], ()> = FxHashMap::default();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:19862:
     na_option: &str,
     pct: bool,
 ) -> Vec<Scalar> {
-    rank_numeric_positions_raw(len, sortable, null_positions, method, ascending, na_option, pct)
-        .into_iter()
-        .map(|rank| {
-            if rank.is_nan() {
-                Scalar::Null(NullKind::NaN)
-            } else {
-                Scalar::Float64(rank)
-            }
-        })
-        .collect()
+    rank_numeric_positions_raw(
+        len,
+        sortable,
+        null_positions,
+        method,
+        ascending,
+        na_option,
+        pct,
+    )
+    .into_iter()
+    .map(|rank| {
+        if rank.is_nan() {
+            Scalar::Null(NullKind::NaN)
+        } else {
+            Scalar::Float64(rank)
+        }
+    })
+    .collect()
 }
 
 /// Core of `rank_numeric_positions` returning the raw `f64` ranks (NaN at
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:48344:
             col_order.push(col_name.clone());
         }
 
-        DataFrame::new_with_axes(
-            out_index,
-            None,
-            result_cols,
-            col_order,
-            None,
-        )
+        DataFrame::new_with_axes(out_index, None, result_cols, col_order, None)
     }
 
     fn plot_series_specs(&self) -> Vec<PlotSeriesSpec> {
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:48473:
             // Timedelta dtype per br-v61uo).
             let f64s = col.as_f64_slice();
             let i64s = col.as_i64_slice();
-            let agg_vals: Vec<Scalar> = if !col.dtype().is_nullable()
-                && (f64s.is_some() || i64s.is_some())
-            {
-                let mut buf: Vec<f64> = Vec::new();
-                let mut out = Vec::with_capacity(n_groups);
-                for gkey in &group_order {
-                    buf.clear();
-                    if let Some(s) = f64s {
-                        for &i in &groups[gkey] {
-                            let v = s[i];
-                            if v.is_finite() {
-                                buf.push(v);
+            let agg_vals: Vec<Scalar> =
+                if !col.dtype().is_nullable() && (f64s.is_some() || i64s.is_some()) {
+                    let mut buf: Vec<f64> = Vec::new();
+                    let mut out = Vec::with_capacity(n_groups);
+                    for gkey in &group_order {
+                        buf.clear();
+                        if let Some(s) = f64s {
+                            for &i in &groups[gkey] {
+                                let v = s[i];
+                                if v.is_finite() {
+                                    buf.push(v);
+                                }
                             }
+                        } else if let Some(s) = i64s {
+                            for &i in &groups[gkey] {
+                                buf.push(s[i] as f64);
+                            }
                         }
-                    } else if let Some(s) = i64s {
-                        for &i in &groups[gkey] {
-                            buf.push(s[i] as f64);
-                        }
+                        out.push(if buf.is_empty() {
+                            Scalar::Null(NullKind::NaN)
+                        } else {
+                            Scalar::Float64(group_nanquantile_f64(&mut buf, q))
+                        });
                     }
-                    out.push(if buf.is_empty() {
-                        Scalar::Null(NullKind::NaN)
-                    } else {
-                        Scalar::Float64(group_nanquantile_f64(&mut buf, q))
-                    });
-                }
-                out
-            } else {
-                let mut agg_vals = Vec::with_capacity(n_groups);
-                for gkey in &group_order {
-                    let row_indices = &groups[gkey];
-                    // Per br-frankenpandas-v61uo: route through fp_types::
-                    // nanquantile so Timedelta64 columns preserve dtype
-                    // (br-5djk7). The previous inline f64 path filtered out
-                    // Timedelta values via to_f64().ok().
-                    let group_scalars: Vec<Scalar> = row_indices
-                        .iter()
-                        .map(|&i| col.values()[i].clone())
-                        .collect();
-                    agg_vals.push(fp_types::nanquantile(&group_scalars, q));
-                }
-                agg_vals
-            };
+                    out
+                } else {
+                    let mut agg_vals = Vec::with_capacity(n_groups);
+                    for gkey in &group_order {
+                        let row_indices = &groups[gkey];
+                        // Per br-frankenpandas-v61uo: route through fp_types::
+                        // nanquantile so Timedelta64 columns preserve dtype
+                        // (br-5djk7). The previous inline f64 path filtered out
+                        // Timedelta values via to_f64().ok().
+                        let group_scalars: Vec<Scalar> = row_indices
+                            .iter()
+                            .map(|&i| col.values()[i].clone())
+                            .collect();
+                        agg_vals.push(fp_types::nanquantile(&group_scalars, q));
+                    }
+                    agg_vals
+                };
 
             result_cols.insert(col_name.clone(), Column::from_values(agg_vals)?);
             col_order.push(col_name.clone());
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:49619:
     ) -> Option<Result<DataFrame, FrameError>> {
         if !matches!(
             func_name,
-            "mean" | "count" | "min" | "max" | "median" | "first" | "last" | "prod" | "var"
-                | "std" | "sum"
+            "mean"
+                | "count"
+                | "min"
+                | "max"
+                | "median"
+                | "first"
+                | "last"
+                | "prod"
+                | "var"
+                | "std"
+                | "sum"
         ) || self.by.is_empty()
         {
             return None;
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:49702:
                         if cnt.iter().all(|&c| c <= 1) {
                             return None;
                         }
-                        let means: Vec<f64> = (0..ngroups).map(|g| sum[g] / cnt[g] as f64).collect();
+                        let means: Vec<f64> =
+                            (0..ngroups).map(|g| sum[g] / cnt[g] as f64).collect();
                         let mut sumsq = vec![0.0_f64; ngroups];
                         for (row, &v) in vals.iter().enumerate() {
                             let g = gid_per_row[row];
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:49827:
                         if cnt.iter().all(|&c| c <= 1) {
                             return None;
                         }
-                        let means: Vec<f64> = (0..ngroups).map(|g| sum[g] / cnt[g] as f64).collect();
+                        let means: Vec<f64> =
+                            (0..ngroups).map(|g| sum[g] / cnt[g] as f64).collect();
                         let mut sumsq = vec![0.0_f64; ngroups];
                         for (row, &v) in vals.iter().enumerate() {
                             let g = gid_per_row[row];
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:49966:
                 return None;
             }
             let (out, mask) = dense_groupby_diff_f64(&gids, ngroups, vals, periods);
-            result_cols.insert(col_name.clone(), Column::from_f64_values_with_validity(out, mask));
+            result_cols.insert(
+                col_name.clone(),
+                Column::from_f64_values_with_validity(out, mask),
+            );
             col_order.push(col_name.clone());
         }
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:50005:
                 return None;
             }
             let (out, mask) = dense_groupby_shift_f64(&gids, ngroups, vals, periods);
-            result_cols.insert(col_name.clone(), Column::from_f64_values_with_validity(out, mask));
+            result_cols.insert(
+                col_name.clone(),
+                Column::from_f64_values_with_validity(out, mask),
+            );
             col_order.push(col_name.clone());
         }
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:50580:
             let typed: Option<Column> = col
                 .as_i64_slice()
                 .and_then(|slice| {
-                    let group_rows: Vec<&[usize]> =
-                        group_order.iter().map(|gkey| groups[gkey].as_slice()).collect();
+                    let group_rows: Vec<&[usize]> = group_order
+                        .iter()
+                        .map(|gkey| groups[gkey].as_slice())
+                        .collect();
                     // Global-argsort O(n) kernel (br-frankenpandas-g0d3l): one
                     // stable radix argsort + CSR scatter into per-group sorted
                     // order + tie-run walk — no per-group histogram/memset. Falls
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:50590:
                         .or_else(|| {
                             let mut out = vec![0.0_f64; n];
                             for rows in &group_rows {
-                                let group_vals: Vec<i64> =
-                                    rows.iter().map(|&i| slice[i]).collect();
+                                let group_vals: Vec<i64> = rows.iter().map(|&i| slice[i]).collect();
                                 let group_ranks =
                                     rank_i64_slice_histogram(&group_vals, method, ascending, pct)?;
                                 for (j, r) in group_ranks.into_iter().enumerate() {
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:50623:
                     let mut out = vec![Scalar::Null(NullKind::NaN); n];
                     for gkey in &group_order {
                         let row_indices = &groups[gkey];
-                        let group_vals: Vec<Scalar> =
-                            row_indices.iter().map(|&i| col.values()[i].clone()).collect();
+                        let group_vals: Vec<Scalar> = row_indices
+                            .iter()
+                            .map(|&i| col.values()[i].clone())
+                            .collect();
                         let mut null_positions = Vec::new();
                         let mut sortable = Vec::new();
                         for (i, v) in group_vals.iter().enumerate() {
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:99291:
         let s = Series::from_values("v", labels, scalars).unwrap();
 
         for &window in &[1usize, 2, 3, 7, 50, 200] {
-            for &mp in &[None, Some(0usize), Some(1), Some(window.min(3)), Some(window)] {
+            for &mp in &[
+                None,
+                Some(0usize),
+                Some(1),
+                Some(window.min(3)),
+                Some(window),
+            ] {
                 let min_periods = mp.unwrap_or(window);
                 if min_periods > window {
                     continue;
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:99303:
                         } else {
                             s.rolling(window, mp)
                         };
-                        let got = if mean { r.mean().unwrap() } else { r.sum().unwrap() };
+                        let got = if mean {
+                            r.mean().unwrap()
+                        } else {
+                            r.sum().unwrap()
+                        };
                         let want = naive(&opt, window, min_periods, center, mean);
                         assert_eq!(got.len(), want.len());
                         for (idx, w) in want.iter().enumerate() {
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:122438:
             ("cumsum", 0.0, |a, v| a + v),
             ("cumprod", 1.0, |a, v| a * v),
             ("cummin", f64::INFINITY, |a, v| if v < a { v } else { a }),
-            ("cummax", f64::NEG_INFINITY, |a, v| if v > a { v } else { a }),
+            (
+                "cummax",
+                f64::NEG_INFINITY,
+                |a, v| if v > a { v } else { a },
+            ),
         ];
         for (name, init, step) in cases {
             let mut acc: std::collections::HashMap<i64, f64> = std::collections::HashMap::new();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:122493:
             ("cumsum", 0.0, |a, v| a + v),
             ("cumprod", 1.0, |a, v| a * v),
             ("cummin", f64::INFINITY, |a, v| if v < a { v } else { a }),
-            ("cummax", f64::NEG_INFINITY, |a, v| if v > a { v } else { a }),
+            (
+                "cummax",
+                f64::NEG_INFINITY,
+                |a, v| if v > a { v } else { a },
+            ),
         ];
         for (name, init, step) in cases {
             let grouped = df.groupby(&["k"]).unwrap();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:122579:
         .unwrap();
         for periods in [1i64, 2] {
             let got = value.groupby(&key).unwrap().shift(periods).unwrap();
-            check(&got.column, &reference(&v0, periods as usize), &format!("series p={periods}"));
+            check(
+                &got.column,
+                &reference(&v0, periods as usize),
+                &format!("series p={periods}"),
+            );
         }
 
         let mut map = BTreeMap::new();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:122590:
         let df = DataFrame::new_with_column_order(Index::new(labels), map, order).unwrap();
         for periods in [1i64, 2] {
             let got = df.groupby(&["k"]).unwrap().shift(periods).unwrap();
-            check(&got.columns["v0"], &reference(&v0, periods as usize), &format!("df v0 p={periods}"));
-            check(&got.columns["v1"], &reference(&v1, periods as usize), &format!("df v1 p={periods}"));
+            check(
+                &got.columns["v0"],
+                &reference(&v0, periods as usize),
+                &format!("df v0 p={periods}"),
+            );
+            check(
+                &got.columns["v1"],
+                &reference(&v1, periods as usize),
+                &format!("df v1 p={periods}"),
+            );
         }
     }
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:122602:
         // `periods` rows of each group missing. Covers periods 1 and 2, a group
         // smaller than periods (all-null), and an inf (inf-inf == NaN valid).
         let keys: Vec<i64> = vec![0, 1, 0, 2, 1, 0, 2, 0, 1, 0];
-        let v0: Vec<f64> = vec![1.0, 5.0, 3.0, f64::INFINITY, 2.0, -1.0, f64::INFINITY, 0.0, 2.0, 4.0];
+        let v0: Vec<f64> = vec![
+            1.0,
+            5.0,
+            3.0,
+            f64::INFINITY,
+            2.0,
+            -1.0,
+            f64::INFINITY,
+            0.0,
+            2.0,
+            4.0,
+        ];
         let v1: Vec<f64> = vec![2.0, 1.5, -4.0, 0.5, 9.0, -3.0, 1.0, 7.0, -0.0, 2.0];
         let n = keys.len();
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:122653:
         .unwrap();
         for periods in [1usize, 2] {
             let got = value.groupby(&key).unwrap().diff(periods).unwrap();
-            check(&got.column, &reference(&v0, periods), &format!("series p={periods}"));
+            check(
+                &got.column,
+                &reference(&v0, periods),
+                &format!("series p={periods}"),
+            );
         }
 
         let mut map = BTreeMap::new();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-frame/src/lib.rs:122664:
         let df = DataFrame::new_with_column_order(Index::new(labels), map, order).unwrap();
         for periods in [1usize, 2] {
             let got = df.groupby(&["k"]).unwrap().diff(periods).unwrap();
-            check(&got.columns["v0"], &reference(&v0, periods), &format!("df v0 p={periods}"));
-            check(&got.columns["v1"], &reference(&v1, periods), &format!("df v1 p={periods}"));
+            check(
+                &got.columns["v0"],
+                &reference(&v0, periods),
+                &format!("df v0 p={periods}"),
+            );
+            check(
+                &got.columns["v1"],
+                &reference(&v1, periods),
+                &format!("df v1 p={periods}"),
+            );
         }
     }
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_dedup.rs:8:
 //! forces the Vec<IndexLabel>-key baseline; the printed `chk` (FNV digest of the
 //! duplicated mask + kept count) must match between the two runs.
 
-use std::hint::black_box;
-use std::time::Instant;
+use std::{hint::black_box, time::Instant};
 
 use fp_index::{DuplicateKeep, IndexLabel, MultiIndex};
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_dedup.rs:16:
 fn main() {
     let args: Vec<String> = std::env::args().collect();
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let iters: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(10);
 
     // ~50% duplicate tuples: low-cardinality 2 Utf8 levels.
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_dedup.rs:22:
-    let l0: Vec<IndexLabel> = (0..n).map(|i| IndexLabel::Utf8(format!("g{}", i % 700))).collect();
-    let l1: Vec<IndexLabel> = (0..n).map(|i| IndexLabel::Utf8(format!("h{}", (i / 7) % 700))).collect();
+    let l0: Vec<IndexLabel> = (0..n)
+        .map(|i| IndexLabel::Utf8(format!("g{}", i % 700)))
+        .collect();
+    let l1: Vec<IndexLabel> = (0..n)
+        .map(|i| IndexLabel::Utf8(format!("h{}", (i / 7) % 700)))
+        .collect();
     let mi = MultiIndex::from_arrays(vec![l0, l1]).expect("mi");
 
     let mask = mi.duplicated(DuplicateKeep::First);
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_get_indexer.rs:8:
 //! the Vec<IndexLabel>-key baseline; the printed `chk` (FNV digest of the
 //! indexer) must match between the two runs.
 
-use std::hint::black_box;
-use std::time::Instant;
+use std::{hint::black_box, time::Instant};
 
 use fp_index::{IndexLabel, MultiIndex};
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_get_indexer.rs:16:
 fn main() {
     let args: Vec<String> = std::env::args().collect();
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let iters: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(20);
     let k = 1000usize; // k * (n/k) unique 2-level tuples
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_get_indexer.rs:22:
-    let l0: Vec<IndexLabel> = (0..n).map(|i| IndexLabel::Utf8(format!("a{}", i / k))).collect();
-    let l1: Vec<IndexLabel> = (0..n).map(|i| IndexLabel::Utf8(format!("b{}", i % k))).collect();
+    let l0: Vec<IndexLabel> = (0..n)
+        .map(|i| IndexLabel::Utf8(format!("a{}", i / k)))
+        .collect();
+    let l1: Vec<IndexLabel> = (0..n)
+        .map(|i| IndexLabel::Utf8(format!("b{}", i % k)))
+        .collect();
     let source = MultiIndex::from_arrays(vec![l0, l1]).expect("source");
     // Target = same tuples reversed (all present) -> full lookup, all hits.
-    let t0: Vec<IndexLabel> = (0..n).rev().map(|i| IndexLabel::Utf8(format!("a{}", i / k))).collect();
-    let t1: Vec<IndexLabel> = (0..n).rev().map(|i| IndexLabel::Utf8(format!("b{}", i % k))).collect();
+    let t0: Vec<IndexLabel> = (0..n)
+        .rev()
+        .map(|i| IndexLabel::Utf8(format!("a{}", i / k)))
+        .collect();
+    let t1: Vec<IndexLabel> = (0..n)
+        .rev()
+        .map(|i| IndexLabel::Utf8(format!("b{}", i % k)))
+        .collect();
     let target = MultiIndex::from_arrays(vec![t0, t1]).expect("target");
 
     let indexer = source.get_indexer(&target).expect("get_indexer");
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_setop.rs:8:
 //! `FP_NO_MISETOP=1` forces the to_list baseline; the printed `chk` (FNV digest
 //! of the result tuples) must match between the two runs.
 
-use std::hint::black_box;
-use std::time::Instant;
+use std::{hint::black_box, time::Instant};
 
 use fp_index::{IndexLabel, MultiIndex};
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_setop.rs:16:
 fn mk(n: usize, off: usize) -> MultiIndex {
-    let l0: Vec<IndexLabel> = (0..n).map(|i| IndexLabel::Utf8(format!("g{}", (i + off) % 700))).collect();
-    let l1: Vec<IndexLabel> = (0..n).map(|i| IndexLabel::Utf8(format!("h{}", ((i + off) / 7) % 700))).collect();
+    let l0: Vec<IndexLabel> = (0..n)
+        .map(|i| IndexLabel::Utf8(format!("g{}", (i + off) % 700)))
+        .collect();
+    let l1: Vec<IndexLabel> = (0..n)
+        .map(|i| IndexLabel::Utf8(format!("h{}", ((i + off) / 7) % 700)))
+        .collect();
     MultiIndex::from_arrays(vec![l0, l1]).expect("mi")
 }
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_setop.rs:24:
     for tuple in mi.to_list() {
         for lbl in tuple {
             let h = match lbl {
-                IndexLabel::Utf8(s) => s.bytes().fold(1469598103934665603u64, |a, b| (a ^ b as u64).wrapping_mul(0x100000001b3)),
+                IndexLabel::Utf8(s) => s.bytes().fold(1469598103934665603u64, |a, b| {
+                    (a ^ b as u64).wrapping_mul(0x100000001b3)
+                }),
                 IndexLabel::Int64(v) => v as u64,
                 _ => 0xDEAD,
             };
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_setop.rs:36:
 
 fn main() {
     let args: Vec<String> = std::env::args().collect();
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let iters: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(10);
     let a = mk(n, 0);
     let b = mk(n, n / 3); // ~partial overlap
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_setop.rs:43:
 
-    let chk = digest(&a.intersection(&b).unwrap()) ^ digest(&a.difference(&b).unwrap()).rotate_left(1);
+    let chk =
+        digest(&a.intersection(&b).unwrap()) ^ digest(&a.difference(&b).unwrap()).rotate_left(1);
 
     let mut sink = 0usize;
     let start = Instant::now();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_sort.rs:7:
 //! tuple-comparison baseline; the printed `chk` (FNV digest of the argsort
 //! permutation) must match between the two runs.
 
-use std::hint::black_box;
-use std::time::Instant;
+use std::{hint::black_box, time::Instant};
 
 use fp_index::{IndexLabel, MultiIndex};
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/bench_multiindex_sort.rs:15:
 fn main() {
     let args: Vec<String> = std::env::args().collect();
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let iters: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(10);
     let k = 1000usize;
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/datetime_get_loc_bench.rs:5:
 //! keeps the linear path — the baseline for what every datetime get_loc did
 //! before.
 
+use std::{hint::black_box, time::Instant};
+
 use fp_index::DatetimeIndex;
-use std::hint::black_box;
-use std::time::Instant;
 
 fn main() {
     let args: Vec<String> = std::env::args().collect();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/datetime_get_loc_bench.rs:14:
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let queries: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(2_000);
 
     let sorted: Vec<i64> = (0..n).map(|i| i as i64 * 1000).collect();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/datetime_get_loc_bench.rs:25:
         .collect();
 
     let mode = args.get(3).map(String::as_str).unwrap_or("sorted");
-    let target = if mode == "linear" { &dt_unsorted } else { &dt_sorted };
+    let target = if mode == "linear" {
+        &dt_unsorted
+    } else {
+        &dt_sorted
+    };
 
     let mut sink = 0usize;
     let start = Instant::now();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/get_indexer_bench.rs:4:
 //!
 //! `FP_NO_IDXDUP=1` forces the old FxHashMap path for the baseline measurement.
 
+use std::{hint::black_box, time::Instant};
+
 use fp_index::{Index, IndexLabel};
-use std::hint::black_box;
-use std::time::Instant;
 
 fn main() {
     let args: Vec<String> = std::env::args().collect();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/get_indexer_bench.rs:13:
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let iters: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(100);
 
     // Sorted self and sorted target with ~50% overlap.
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/get_indexer_bench.rs:17:
     let a: Vec<IndexLabel> = (0..n).map(|i| IndexLabel::Int64(i as i64 * 2)).collect();
-    let t: Vec<IndexLabel> = (0..n).map(|i| IndexLabel::Int64(i as i64 * 2 + 1)).collect();
+    let t: Vec<IndexLabel> = (0..n)
+        .map(|i| IndexLabel::Int64(i as i64 * 2 + 1))
+        .collect();
     let ia = Index::new(a);
     let target = Index::new(t);
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/has_duplicates_bench.rs:5:
 //!
 //! `FP_NO_IDXDUP=1` forces the old FxHashMap path for the baseline measurement.
 
+use std::{hint::black_box, time::Instant};
+
 use fp_index::{Index, IndexLabel};
-use std::hint::black_box;
-use std::time::Instant;
 
 fn main() {
     let args: Vec<String> = std::env::args().collect();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/has_duplicates_bench.rs:14:
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let iters: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(200);
 
     // Strictly ascending, gapped (so it is a materialized label vector, not a
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/intersection_bench.rs:4:
 //!
 //! `FP_NO_IDXDUP=1` forces the old FxHashMap path for the baseline measurement.
 
+use std::{hint::black_box, time::Instant};
+
 use fp_index::{Index, IndexLabel};
-use std::hint::black_box;
-use std::time::Instant;
 
 fn main() {
     let args: Vec<String> = std::env::args().collect();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/intersection_bench.rs:13:
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let iters: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(200);
 
     // Two strictly-ascending Int64 indexes with ~50% overlap.
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/unique_bench.rs:5:
 //!
 //! `FP_NO_IDXDUP=1` forces the old FxHashMap path for the baseline measurement.
 
+use std::{hint::black_box, time::Instant};
+
 use fp_index::{Index, IndexLabel};
-use std::hint::black_box;
-use std::time::Instant;
 
 fn main() {
     let args: Vec<String> = std::env::args().collect();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/examples/unique_bench.rs:14:
-    let n: usize = args.get(1).and_then(|s| s.parse().ok()).unwrap_or(1_000_000);
+    let n: usize = args
+        .get(1)
+        .and_then(|s| s.parse().ok())
+        .unwrap_or(1_000_000);
     let iters: usize = args.get(2).and_then(|s| s.parse().ok()).unwrap_or(200);
 
     let labels: Vec<IndexLabel> = (0..n).map(|i| IndexLabel::Int64(i as i64 * 2)).collect();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:12616:
             // Dedup to DISTINCT values first (O(n) hash), then sort only those
             // (O(d log d)); sorting all n refs would cost as much as the tuple
             // sort we are replacing.
-            let mut sorted: Vec<&IndexLabel> = col.iter().collect::<FxHashSet<_>>().into_iter().collect();
+            let mut sorted: Vec<&IndexLabel> =
+                col.iter().collect::<FxHashSet<_>>().into_iter().collect();
             sorted.sort_unstable();
             let radix = sorted.len() as u64;
             let mut rank: FxHashMap<&IndexLabel, u64> =
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:12735:
         }
 
         if let Some((src_keys, tgt_keys)) = self.factorize_packed_keys(target) {
-            let mut positions =
-                FxHashMap::<u64, Vec<usize>>::with_capacity_and_hasher(self.len(), Default::default());
+            let mut positions = FxHashMap::<u64, Vec<usize>>::with_capacity_and_hasher(
+                self.len(),
+                Default::default(),
+            );
             for (row, &key) in src_keys.iter().enumerate() {
                 positions.entry(key).or_default().push(row);
             }
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:12800:
         }
 
         if let Some((src_keys, tgt_keys)) = self.factorize_packed_keys(target) {
-            let mut positions = FxHashMap::<u64, isize>::with_capacity_and_hasher(
-                self.len(),
-                Default::default(),
-            );
+            let mut positions =
+                FxHashMap::<u64, isize>::with_capacity_and_hasher(self.len(), Default::default());
             for (row, &key) in src_keys.iter().enumerate() {
                 positions
                     .entry(key)
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:13205:
         // SipHash HashMap<Vec<IndexLabel>>. Keep self rows whose key is in other,
         // deduped first-seen, then gather those positions. Bijective on tuple
         // identity ⇒ same kept rows, same order.
-        if let Some((self_keys, other_keys)) = self.factorize_packed_keys(other)
-        {
+        if let Some((self_keys, other_keys)) = self.factorize_packed_keys(other) {
             let other_set: FxHashSet<u64> = other_keys.into_iter().collect();
             let mut seen: FxHashSet<u64> =
                 FxHashSet::with_capacity_and_hasher(self_keys.len(), Default::default());
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:13258:
         self.ensure_same_nlevels(other)?;
         // Packed-key fast path (br-frankenpandas-misetop): keep self rows whose
         // key is NOT in other, deduped first-seen. See intersection.
-        if let Some((self_keys, other_keys)) = self.factorize_packed_keys(other)
-        {
+        if let Some((self_keys, other_keys)) = self.factorize_packed_keys(other) {
             let other_set: FxHashSet<u64> = other_keys.into_iter().collect();
             let mut seen: FxHashSet<u64> =
                 FxHashSet::with_capacity_and_hasher(self_keys.len(), Default::default());
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:13912:
                 .filter(|l| seen.insert((*l).clone()))
                 .cloned()
                 .collect();
-            assert_eq!(idx.unique().labels(), ref_unique.as_slice(), "unique {labels:?}");
+            assert_eq!(
+                idx.unique().labels(),
+                ref_unique.as_slice(),
+                "unique {labels:?}"
+            );
 
             let mut seen_f = std::collections::HashSet::new();
-            let ref_dup_first: Vec<bool> = labels
-                .iter()
-                .map(|l| !seen_f.insert(l.clone()))
-                .collect();
+            let ref_dup_first: Vec<bool> =
+                labels.iter().map(|l| !seen_f.insert(l.clone())).collect();
             assert_eq!(
                 idx.duplicated(DuplicateKeep::First),
                 ref_dup_first,
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:13940:
         // strictly ascending) or the hash path runs (any side unsorted).
         let s = |v: &[i64]| v.iter().map(|x| IndexLabel::Int64(*x)).collect::<Vec<_>>();
         let pairs: Vec<(Vec<IndexLabel>, Vec<IndexLabel>)> = vec![
-            (s(&[1, 2, 3, 5]), s(&[2, 3, 4])),       // both sorted, overlap
-            (s(&[1, 2, 3]), s(&[4, 5, 6])),          // both sorted, disjoint
-            (s(&[1, 2, 3]), s(&[1, 2, 3])),          // identical
-            (s(&[1, 2, 3]), vec![]),                 // empty other
-            (vec![], s(&[1, 2, 3])),                 // empty self
-            (s(&[3, 1, 2]), s(&[2, 3])),             // self unsorted -> hash path
-            (s(&[1, 2, 3]), s(&[3, 1])),             // other unsorted -> hash path
-            (vec!["a".into(), "c".into(), "e".into()], vec!["b".into(), "c".into()]), // utf8 sorted
+            (s(&[1, 2, 3, 5]), s(&[2, 3, 4])), // both sorted, overlap
+            (s(&[1, 2, 3]), s(&[4, 5, 6])),    // both sorted, disjoint
+            (s(&[1, 2, 3]), s(&[1, 2, 3])),    // identical
+            (s(&[1, 2, 3]), vec![]),           // empty other
+            (vec![], s(&[1, 2, 3])),           // empty self
+            (s(&[3, 1, 2]), s(&[2, 3])),       // self unsorted -> hash path
+            (s(&[1, 2, 3]), s(&[3, 1])),       // other unsorted -> hash path
             (
+                vec!["a".into(), "c".into(), "e".into()],
+                vec!["b".into(), "c".into()],
+            ), // utf8 sorted
+            (
                 vec![1_i64.into(), 2_i64.into()],
                 vec!["a".into(), "b".into()],
             ), // mixed-type sorted, disjoint by Ord variant
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:13997:
             let td = TimedeltaIndex::new(nanos.clone());
             for q in [10_i64, 20, 30, 40, 50, 0, 99] {
                 let expected = nanos.iter().position(|n| *n == q);
-                assert_eq!(dt.get_loc(q).ok(), expected, "datetime nanos={nanos:?} q={q}");
-                assert_eq!(td.get_loc(q).ok(), expected, "timedelta nanos={nanos:?} q={q}");
+                assert_eq!(
+                    dt.get_loc(q).ok(),
+                    expected,
+                    "datetime nanos={nanos:?} q={q}"
+                );
+                assert_eq!(
+                    td.get_loc(q).ok(),
+                    expected,
+                    "timedelta nanos={nanos:?} q={q}"
+                );
             }
         }
     }
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:14009:
         // FxHashMap fallback must all equal a first-occurrence reference.
         let s = |v: &[i64]| v.iter().map(|x| IndexLabel::Int64(*x)).collect::<Vec<_>>();
         let cases: Vec<(Vec<IndexLabel>, Vec<IndexLabel>)> = vec![
-            (s(&[1, 2, 3, 4, 5]), s(&[2, 4, 6])),     // both sorted
-            (s(&[1, 2, 3, 4, 5]), s(&[5, 1, 3, 9])),  // self sorted, target unsorted
-            (s(&[3, 1, 5, 2]), s(&[1, 2, 3])),        // self unsorted -> hash path
-            (s(&[1, 2, 3]), vec![]),                  // empty target
-            (vec![], s(&[1, 2])),                     // empty self
+            (s(&[1, 2, 3, 4, 5]), s(&[2, 4, 6])),    // both sorted
+            (s(&[1, 2, 3, 4, 5]), s(&[5, 1, 3, 9])), // self sorted, target unsorted
+            (s(&[3, 1, 5, 2]), s(&[1, 2, 3])),       // self unsorted -> hash path
+            (s(&[1, 2, 3]), vec![]),                 // empty target
+            (vec![], s(&[1, 2])),                    // empty self
             (
                 vec!["a".into(), "c".into(), "e".into()],
                 vec!["c".into(), "z".into(), "a".into()],
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:14020:
             ), // utf8, target unsorted
-            (s(&[1, 2, 3]), vec!["a".into()]), // mixed type sorted, no match
+            (s(&[1, 2, 3]), vec!["a".into()]),       // mixed type sorted, no match
         ];
         for (a, b) in cases {
             let ia = Index::new(a.clone());
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:17330:
         for (sa, sb) in cases {
             let a = mk(&sa);
             let b = mk(&sb);
-            let bset: std::collections::HashSet<Vec<IndexLabel>> = b.to_list().into_iter().collect();
+            let bset: std::collections::HashSet<Vec<IndexLabel>> =
+                b.to_list().into_iter().collect();
 
             let mut seen = std::collections::HashSet::new();
             let ref_inter: Vec<Vec<IndexLabel>> = a
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:17338:
                 .into_iter()
                 .filter(|t| bset.contains(t) && seen.insert(t.clone()))
                 .collect();
-            assert_eq!(a.intersection(&b).unwrap().to_list(), ref_inter, "inter {sa:?}");
+            assert_eq!(
+                a.intersection(&b).unwrap().to_list(),
+                ref_inter,
+                "inter {sa:?}"
+            );
 
             let mut seen_d = std::collections::HashSet::new();
             let ref_diff: Vec<Vec<IndexLabel>> = a
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:17360:
         let mut l0 = Vec::with_capacity(n);
         let mut l1 = Vec::with_capacity(n);
         for _ in 0..n {
-            state = state.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
+            state = state
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
             l0.push(IndexLabel::Utf8(format!("g{}", (state >> 40) % 6)));
-            state = state.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
+            state = state
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
             l1.push(IndexLabel::Int64(((state >> 40) % 5) as i64));
         }
         let mi = MultiIndex::from_arrays(vec![l0, l1]).unwrap();
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:17369:
         let rows = mi.to_list();
 
-        for keep in [DuplicateKeep::First, DuplicateKeep::Last, DuplicateKeep::None] {
+        for keep in [
+            DuplicateKeep::First,
+            DuplicateKeep::Last,
+            DuplicateKeep::None,
+        ] {
             let mut want = vec![false; n];
             match keep {
                 DuplicateKeep::First => {
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:17423:
         let mut l0 = Vec::with_capacity(n);
         let mut l1 = Vec::with_capacity(n);
         for _ in 0..n {
-            state = state.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
+            state = state
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
             let a = (state >> 33) % 7; // low cardinality -> duplicate tuples
-            state = state.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
+            state = state
+                .wrapping_mul(6364136223846793005)
+                .wrapping_add(1442695040888963407);
             let b = (state >> 33) % 5;
             l0.push(IndexLabel::Utf8(format!("g{a}")));
             l1.push(IndexLabel::Int64(b as i64));
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:17438:
         want.sort_by(|&a, &b| rows[a].cmp(&rows[b]).then(a.cmp(&b)));
 
         assert_eq!(mi.argsort(), want, "argsort");
-        assert_eq!(mi.sort_values().to_list(), mi.take_existing_positions(&want).to_list());
+        assert_eq!(
+            mi.sort_values().to_list(),
+            mi.take_existing_positions(&want).to_list()
+        );
         // min/max derive from argsort and must match the reference ends.
         assert_eq!(mi.min(), Some(rows[want[0]].clone()));
         assert_eq!(mi.max(), Some(rows[want[n - 1]].clone()));
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-index/src/lib.rs:17458:
             .unwrap()
         };
         let source = mk(&[("a", 1), ("a", 2), ("b", 1), ("a", 1), ("c", 5), ("b", 2)]);
-        let target = mk(&[("b", 1), ("z", 9), ("a", 1), ("a", 2), ("c", 5), ("q", 0), ("b", 2)]);
+        let target = mk(&[
+            ("b", 1),
+            ("z", 9),
+            ("a", 1),
+            ("a", 2),
+            ("c", 5),
+            ("q", 0),
+            ("b", 2),
+        ]);
         let src_rows = source.to_list();
         let tgt_rows = target.to_list();
 
Diff in /data/projects/.scratch/frankenpandas-orangepeak-3wo1d-current-20260609/crates/fp-io/src/lib.rs:1740:
         return None;
     }
     let delim_c = delim as char;
-    let needs_quote =
-        |s: &str| s.bytes().any(|b| b == delim || b == b'"' || b == b'\n' || b == b'\r');
+    let needs_quote = |s: &str| {
+        s.bytes()
+            .any(|b| b == delim || b == b'"' || b == b'\n' || b == b'\r')
+    };
 
     let headers: Vec<&String> = frame.column_names().into_iter().collect();
     if headers.iter().any(|h| needs_quote(h)) {
