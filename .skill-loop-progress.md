# Skill Loop Progress
# Skill: extreme-software-optimization + alien-graveyard + alien-artifact-coding
# Target: br-frankenpandas-typed-columnar-storage-epic-fi6zx + br-frankenpandas-w4vff + br-frankenpandas-w8826 + br-frankenpandas-uza04 + br-frankenpandas-j5gqz + br-frankenpandas-n921e
# Total Passes: 27
# Started: 2026-06-02T23:07:00Z

## Status: IN PROGRESS - Pass 27 retained; shifted-bottleneck reprofile ranks series_add_align ahead of residual csv_read and join_1to1

## Missions
1. Target selection and coordination: claim typed-columnar epic, reserve storage/frame/tracker files, select a profile-backed copy-wall lever.
2. Baseline and golden proof: capture RCH hyperfine baselines and deterministic golden sha256 before editing.
3. Single-lever implementation: probe the shared-storage copy wall, reject it when it regresses, then retain the narrower all-valid primitive gather specialization.
4. Rebench and validation: rerun RCH hyperfine, golden sha, and crate-scoped checks; keep only if Score >= 2.0.
5. Closeout and re-profile: commit with before/after, update/close bead phase, push main and main:master, then re-check perf queue.
6. Clone cache isomorphism: finish the in-flight private ColumnData clone-cache lever with RCH benchmark, golden sha256, focused tests, and bead/mail closeout if it passes Score >= 2.0.
7. Fresh typed-gather profile: re-profile filter_bool after pass 8, then target only the top self-time gather/materialization hotspot with one typed-columnar lever.
8. Sort-key profile: re-profile sort_single and target only a profile-proven comparator or gather hotspot while preserving ordering and tie-breaks.
9. Drop-duplicates profile: re-profile drop_duplicates and target only a measured key-building or materialization hotspot with golden output proof.
10. Shifted-bottleneck audit: re-rank filter/sort/drop_duplicates after prior passes, reject subthreshold candidates, and file/claim the next [perf] bead only with profile evidence.
11. Regression discipline: record rejected filter_bool mask-gather candidate evidence and keep no source diff when Score < 2.0.
12. Noise discipline: reject small filter_bool wins whose paired confidence interval overlaps no improvement.
13. Index-materialization discipline: reject row-index allocation shortcuts unless paired A/B proves a real filter_bool win.
14. Bounds-check discipline: reject loc_bool unchecked routing unless paired A/B proves it actually moves the hot workload.
15. Lazy scalar materialization discipline: keep typed Float64 gather data contiguous and materialize public Scalar views only when observation requires it.
16. Lazy clone discipline: keep all-valid Float64 clone output typed-backed and defer Scalar materialization until observation.
17. Post-clone reprofile discipline: re-rank standard perf workloads after the retained clone lever and avoid the join hotspot while fi6zx.11 is already in progress.

## Completed Passes
### Pass 1 - Target Selection And Coordination - 2026-06-02T23:07:00Z
- Bead claimed: br-frankenpandas-typed-columnar-storage-epic-fi6zx.
- Reservations: crates/fp-columnar/src/lib.rs, crates/fp-frame/src/lib.rs, .beads/issues.jsonl, .skill-loop-progress.md.
- Profile-backed target: typed-columnar bead baseline says filter_bool is 11.5x behind pandas and Column::take_positions Scalar AoS gather is 71% self-time; post-ve6bl sorted sort_values is also clone/copy-heavy.
- Alien primitive: SoA/vectorized execution (§8.2) remains the long-term target; this phase applies the cache/memory-copy subprimitive by making scalar buffers immutable shared storage while preserving public scalar slices.
- Artifact proof obligations: output order unchanged, no arithmetic/FP mutation, tie-breaking unchanged, RNG absent, null-kind values are byte-identical because scalar elements are shared/copied unchanged and serialization still emits the same values.
- Score hypothesis: Impact 2 x Confidence 4 / Effort 1 = 8.0 if clone-heavy paths clear RCH A/B.
- Verdict: PRODUCTIVE

### Pass 2 - Baseline And Golden Proof - 2026-06-02T23:14:00Z
- Baseline build: RCH cargo build -p fp-conformance --profile release-perf --example perf_profile passed.
- Baseline filter_bool: RCH hyperfine `filter_bool 100000 200`, mean 883.2 ms, stddev 35.6 ms, min 817.4 ms, max 929.1 ms.
- Baseline sort_single: RCH hyperfine `sort_single 100000 70`, mean 283.0 ms, stddev 12.3 ms, min 262.3 ms, max 301.6 ms.
- Golden before filter_bool 5000 sha256: eeeba6308a221e0209f4b29930d0e6f27115bff38885379d061e2a36114616d7.
- Golden before sort_single 5000 sha256: 0d34cbf3261960c869ee85a7a91641c038e9e2f7c253ca2dec701075da028493.
- Isomorphism obligations: storage sharing must not change scalar sequence, dtype, validity mask, row order, stable tie order, FP bits, null kind, error class, or RNG state; serde must keep the old Column `{dtype, values, validity}` shape.
- Verdict: PRODUCTIVE

### Pass 3 - Single-Lever Implementation - 2026-06-02T23:18:00Z
- Files changed: crates/fp-columnar/src/lib.rs.
- Lever: Column now stores scalar buffers in a private `SharedScalars(Arc<[Scalar]>)` wrapper that derefs/iterates as `[Scalar]`; clone/view/transpose/promote/with_dtype now share immutable scalar storage instead of deep-copying every Scalar.
- Owned mutation/materialization call sites (`to_vec`, `flatten`, `put`, `reverse`, `union1d`) explicitly call `to_vec()` so behavior remains copy-owning where pandas semantics require a new mutable buffer.
- Serialization remains the prior observable shape by manually serializing `dtype`, scalar `values`, and `validity`; deserialization rebuilds the shared immutable buffer without changing scalar values.
- Local smoke: `cargo check -p fp-columnar --lib` passed.
- Isomorphism proof: values are the same `Scalar` sequence in the same order; validity mask is unchanged; no arithmetic is performed; FP bits and null-kind variants are preserved by sharing/cloning the same scalar values; RNG is absent; ordering/tie-break logic is untouched.
- Score hypothesis remains 8.0 pending RCH rebench.
- Verdict: PRODUCTIVE

### Pass 4 - Rebench And Validation - 2026-06-03T00:22:00Z
- Files retained: none from `crates/fp-columnar/src/lib.rs`; the storage rewrite was backed out after measurement.
- RCH build: `cargo build -p fp-conformance --profile release-perf --example perf_profile` passed for the candidate binary.
- Rebench result: `filter_bool 100000 200` mean 2.778s, stddev 0.053s, min 2.682s, max 2.848s.
- Baseline comparison: saved before mean 0.883s; candidate was ~3.15x slower on the top profile target.
- Validation finding: candidate also produced unused `SharedScalars::from_typed` / `data` warnings, showing the hot path had not moved to typed backing.
- Decision: reject this scalar-copy storage lever. Golden-output proof was not run because the performance gate failed first; no code commit was made.
- Next target: keep `br-frankenpandas-typed-columnar-storage-epic-fi6zx` open/in-progress and use the same profile evidence to pursue a narrower typed gather/materialization lever.
- Verdict: REJECTED

### Pass 5 - Mask-Generated Positions Validation Bypass - 2026-06-03T02:13:00Z
- Candidate bead: br-frankenpandas-0ejxb.1, child of br-frankenpandas-0ejxb.
- Candidate lever: `DataFrame::loc_bool` would build positions from the already length-checked mask and call the existing unchecked internal row materializer.
- Initial RCH proof was promising: `filter_bool 100000 200` moved from saved baseline 883.2 ms to 858.8 ms, paired A/B `100000x500` was 2.151s +/-0.049 -> 2.084s +/-0.070, and golden filter_bool n=0/1/7/5000 matched.
- Final closeout correction: this frame-side source diff did not remain in the final staged code after the next profile pass. The child bead was reopened and reclosed as backed out/not shipped before commit.
- Files retained from this pass: none.
- Score: not counted; no source diff retained.
- Verdict: REJECTED

### Pass 6 - Primitive All-Valid Gather Specialization - 2026-06-03T02:23:00Z
- Bead claimed: `br-frankenpandas-0ejxb`; scope reserved for `crates/fp-columnar/src/lib.rs`, `.beads/issues.jsonl`, `.skill-loop-progress.md`, and perf artifacts.
- Baseline: RCH rebuilt current-source `perf_profile`, then RCH hyperfine `filter_bool 100000 200` mean 863.2 ms, stddev 43.1 ms, min 794.1 ms, max 939.9 ms.
- Lever: specialize all-valid primitive `Column::take_positions` for Bool/Int64/Float64/Timedelta64/Datetime64/Period payload copies, falling back to the existing `Scalar::clone` path for object/string/interval or unexpected variants.
- Isomorphism proof: output positions are traversed in identical order; emitted scalar variants and primitive payload bits are identical; null-bearing columns stay on the old normalization path; no arithmetic, comparisons, tie-breaking, serialization, or RNG behavior changes.
- Golden-output proof: `perf_profile golden filter_bool 5000` sha256 stayed `eeeba6308a221e0209f4b29930d0e6f27115bff38885379d061e2a36114616d7`.
- Rebench: first RCH hyperfine mean 702.0 ms, stddev 67.7 ms; confirmation RCH hyperfine mean 714.3 ms, stddev 69.4 ms, min 595.4 ms, max 806.6 ms.
- Delta: 863.2 ms -> 714.3 ms, 17.25% faster, 1.21x speedup on the top profile-backed filter_bool target.
- Validation: RCH `cargo test -p fp-columnar take_positions -- --nocapture`, RCH `cargo check -p fp-columnar --all-targets`, RCH `cargo clippy -p fp-columnar --all-targets -- -D warnings`, local `cargo fmt -p fp-columnar -- --check`, and `ubs crates/fp-columnar/src/lib.rs` all exited 0.
- Score: Impact 3 x Confidence 4 / Effort 1 = 12.0, retained.
- Verdict: PRODUCTIVE

### Pass 7 - All-Unique Drop Duplicates Clone Fast Path - 2026-06-03T03:14:00Z
- Bead claimed: `br-frankenpandas-uza04.1`; scope reserved for `crates/fp-frame/src/lib.rs`, `.beads/issues.jsonl`, `.skill-loop-progress.md`, and drop_duplicates perf artifacts.
- Profile-backed target: fresh RCH-built rank showed `drop_duplicates 100000 80` at 1.390s mean, slower than `filter_bool 100000 200` at 715.5 ms and `sort_single 100000 200` at 741.2 ms; existing hotspot notes attribute meaningful self-time to row materialization after duplicate mask computation.
- Baseline: RCH-built current binary direct hyperfine `drop_duplicates 100000 80` mean 1.221s, stddev 0.031s.
- Lever: after computing the existing `duplicated` mask, `DataFrame::drop_duplicates` returns `self.clone()` only when `ignore_index=false`, every marker is `Bool(false)`, and no duplicate was observed; duplicates, `ignore_index=true`, and unexpected markers stay on the previous row-gather fallback.
- Golden-output proof: `perf_profile golden drop_duplicates` for n=0,1,7,5000 matched before/after sha256 values exactly.
- Isomorphism proof: the duplicate mask is computed identically before the fast-path decision; output row order, index labels, column order, scalar payload bits, null/NaN variants, tie policy, FP arithmetic, and RNG state are unchanged. The reset-index path is deliberately excluded.
- Rebench: paired RCH A/B hyperfine before 1.275s +/- 0.044 vs after 1.144s +/- 0.032, after 1.11x +/- 0.05 faster on the profile-backed target.
- Validation: RCH `cargo test -p fp-frame dataframe_drop_duplicates_all_unique_preserves_observable_state --lib`, RCH `cargo check -p fp-frame --all-targets`, RCH `cargo clippy -p fp-frame --all-targets --no-deps -- -D warnings -A clippy::too_many_arguments`, local `cargo fmt -p fp-frame -- --check`, and `ubs crates/fp-frame/src/lib.rs` timed out after 180s without findings output.
- Score: Impact 2 x Confidence 4 / Effort 1 = 8.0, retained.
- Verdict: PRODUCTIVE

### Pass 8 - Clone Cache Isomorphism - 2026-06-03T01:37:30-04:00
- Bead continued: `br-frankenpandas-typed-columnar-storage-epic-fi6zx`; scope reserved for `crates/fp-columnar/src/lib.rs`, `tests/artifacts/perf/*cached*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Baseline: existing in-flight RCH hyperfine `sort_single 100000 200` mean 761.0 ms, stddev 21.4 ms (`tests/artifacts/perf/fp_before_sort_single_cached_fi6zx_2.json`).
- Lever: custom `Column::clone` now copies observable `dtype`, `values`, and `validity`, but drops the private `ColumnData` cache instead of deep-cloning it.
- Isomorphism proof: `ColumnData` is private and serde-skipped; `PartialEq`, `Debug`, `values()`, `dtype()`, and `validity()` are value-based. Output order, tie-breaking, scalar payload bits, null variants, floating-point bits, error classes, and RNG state are unchanged because no values or comparisons are modified.
- Golden-output proof: `perf_profile golden sort_single` sha256 stayed `0d34cbf3261960c869ee85a7a91641c038e9e2f7c253ca2dec701075da028493`.
- Rebench: RCH-built candidate binary, direct hyperfine `sort_single 100000 200` mean 678.7 ms, stddev 37.0 ms, min 634.1 ms, max 750.3 ms.
- Delta: 761.0 ms -> 678.7 ms, 10.8% faster, 1.12x speedup on the clone-heavy sort target.
- Validation: RCH `cargo test -p fp-columnar column_clone_preserves_values_without_copying_private_cache -- --nocapture`, RCH `cargo check -p fp-columnar --all-targets`, RCH `cargo clippy -p fp-columnar --all-targets -- -D warnings`, and `ubs crates/fp-columnar/src/lib.rs` all exited 0. Global `cargo fmt --check` still reports unrelated formatting drift outside this bead surface; package-scoped fmt check is the relevant gate for this pass.
- Score: Impact 2 x Confidence 4 / Effort 1 = 8.0, retained.
- Verdict: PRODUCTIVE

### Pass 9 - All-Valid Unique Column Drop Duplicates Bypass - 2026-06-03T02:39:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.3`; scope reserved for `crates/fp-frame/src/lib.rs`, `tests/artifacts/perf/*unique_column_drop_duplicates_fi6zx_3*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: after the clone-cache pass, `drop_duplicates 100000 80` remained hot at 1.240s mean, stddev 40.3 ms; the `perf_profile` workload has repeated key column `k` and all-valid unique value column `v`, so row hash construction was provably unnecessary.
- Baseline: RCH-built current binary hyperfine `drop_duplicates 100000 80` mean 1.240s, stddev 40.3 ms, min 1.171s, max 1.294s (`tests/artifacts/perf/fp_before_unique_column_drop_duplicates_fi6zx_3.json`).
- Lever: before computing the duplicated row mask, `DataFrame::drop_duplicates(ignore_index=false)` resolves the selected columns and returns `self.clone()` when any selected all-valid column is unique under the same scalar equality used by row duplicate checks.
- Isomorphism proof: if one selected column is unique, every selected row tuple is unique, so `keep=First`, `keep=Last`, and `keep=None` all retain every input row. Output row order, index labels, column order, scalar payload bits, null/NaN variants, tie policy, and RNG state are unchanged. Missing columns still error through the same selector; `ignore_index=true` and missing/null-bearing columns stay on the old duplicated-mask path. Float64 uniqueness uses the existing `Scalar::semantic_eq` relative tolerance before claiming uniqueness.
- Golden-output proof: `perf_profile golden drop_duplicates 5000` sha256 stayed `64d27a6b11527d27b9c08530971eab1a3b758531a5bf4dd94f6968082d1d0292`.
- Rebench: current-source candidate binary from unique target `/data/tmp/cargo-target-rubygoose-fi6zx3`, hyperfine `drop_duplicates 100000 80` mean 154.7 ms, stddev 9.6 ms, min 142.0 ms, max 173.2 ms.
- Delta: 1.240s -> 154.7 ms, 87.5% faster, 8.02x speedup on the profile-backed drop_duplicates target.
- Validation: RCH `cargo test -p fp-frame dataframe_drop_duplicates_unique -- --nocapture`, RCH `cargo check -p fp-frame --all-targets`, RCH `cargo clippy -p fp-frame --all-targets --no-deps -- -D warnings -A clippy::too_many_arguments`, local `cargo fmt -p fp-frame -- --check`, and before/after golden sha256 all passed. Full dependency clippy is blocked by an unrelated `fp-index` `needless_range_loop` lint; `ubs crates/fp-frame/src/lib.rs` was retried with a 120s timeout on this very large file and timed out with no findings output.
- Score: Impact 4 x Confidence 4 / Effort 1 = 16.0, retained.
- Verdict: PRODUCTIVE

### Pass 10 - Typed Dense Sort Sortedness Check - 2026-06-03T03:10:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.4`; scope reserved for `crates/fp-frame/src/lib.rs`, `tests/artifacts/perf/*sort_single*fi6zx_4*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: after the unique drop-duplicates pass, shifted reprofile ranked `sort_single 100000 200` hottest at 701.4 ms mean, stddev 35.6 ms. Existing `tests/artifacts/perf/HOTSPOT_TABLE.md` identifies `fp_frame::DataFrame::sort_values_na` generic `Scalar` comparison as a major sort hotspot.
- Baseline: saved before binary direct hyperfine `sort_single 100000 200` mean 806.0 ms, stddev 95.5 ms, min 694.4 ms, max 978.4 ms (`tests/artifacts/perf/fp_before_sort_single_fi6zx_4.json`).
- Lever: `DataFrame::sort_values_na` now checks all-valid dense Int64/nullable-Int64/Float64 columns with a typed sortedness scan before the generic `Scalar` window comparator. Nulls, NaNs, malformed mixed values, object/string columns, and unsupported dtypes return `None` and use the prior path.
- Isomorphism proof: the fast path only changes how already-sorted all-valid numeric columns are detected before returning the existing `self.clone()` result. Ascending and descending order use the same `i64::cmp` and `f64::partial_cmp(...).unwrap_or(Equal)` ordering as the existing typed sort helper; equal values keep input order; null/NaN and NA-position-sensitive cases fall back to the old comparator. No scalar payloads, FP bits, row/index order, tie policy, error class, or RNG state are modified.
- Golden-output proof: `perf_profile golden sort_single 5000` sha256 stayed `0d34cbf3261960c869ee85a7a91641c038e9e2f7c253ca2dec701075da028493`; before/after golden files have no diff.
- Rebench: RCH-built candidate binary direct hyperfine mean 685.8 ms, stddev 45.6 ms, min 612.7 ms, max 770.2 ms. Paired A/B hyperfine in the same environment: before 804.1 ms +/- 102.2, after 694.3 ms +/- 28.9; candidate ran 1.16x +/- 0.15 faster.
- Delta: paired result is 804.1 ms -> 694.3 ms, 13.7% faster on the profile-backed sort target.
- Validation: RCH `cargo test -p fp-frame typed_dense_values_already_sorted_matches_supported_dense_semantics --lib -- --nocapture`, RCH `cargo test -p fp-frame dataframe_sort_values --lib -- --nocapture`, RCH `cargo check -p fp-frame --all-targets`, RCH `cargo clippy -p fp-frame --all-targets --no-deps -- -D warnings -A clippy::too_many_arguments`, local `cargo fmt -p fp-frame -- --check`, and before/after golden sha256 all passed. `ubs crates/fp-frame/src/lib.rs` was run under a 180s timeout and timed out with no findings output or leftover child processes.
- Score: Impact 2 x Confidence 3 / Effort 1 = 6.0, retained.
- Verdict: PRODUCTIVE

### Pass 11 - Dense Primitive Clone From Typed Cache - 2026-06-03T03:48:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.5`; scope reserved for `crates/fp-columnar/src/lib.rs`, `tests/artifacts/perf/*sort*fi6zx_5*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: post-pass-10 reprofile ranked `sort_single 100000 200` hottest at 661.8 ms mean, ahead of `filter_bool 100000 200` at 465.0 ms and `drop_duplicates 100000 80` at 160.4 ms. Fresh GDB sampling on `sort_single 100000 5000` captured 59/80 samples with `Vec<Scalar>::clone -> fp_columnar::Column::clone -> BTreeMap clone -> DataFrame::clone -> sort_values_na`.
- Baseline: current RCH-built binary direct hyperfine `sort_single 100000 200` mean 647.3 ms, stddev 35.3 ms, min 587.1 ms, max 702.8 ms (`tests/artifacts/perf/fp_before_sort_clone_fi6zx_5.json`).
- Lever: `Column::clone` now reconstructs all-valid primitive scalar values from the existing private typed `ColumnData` cache for Bool, Int64, Float64, Timedelta64, Datetime64, and Period columns, then still drops the private cache in the clone. Missing/null-bearing, unsupported, mismatched, or malformed cache states fall back to the prior scalar-vector clone.
- Isomorphism proof: the cloned public `dtype`, scalar value sequence, validity mask, and private-cache-dropping behavior remain observable-equivalent. Row order, index labels, tie-breaking, null/NaN variants, floating-point payload bits including `-0.0`, error classes, serialization shape, and RNG state are unchanged because no comparisons, arithmetic, ordering, or random state are touched.
- Golden-output proof: `perf_profile golden sort_single 5000` sha256 stayed `0d34cbf3261960c869ee85a7a91641c038e9e2f7c253ca2dec701075da028493`; before/after golden files have no diff.
- Rebench: RCH-built candidate binary direct hyperfine mean 230.6 ms, stddev 32.2 ms, min 192.3 ms, max 279.9 ms. Paired A/B hyperfine in the same environment: before 636.1 ms +/- 32.1, after 210.3 ms +/- 21.3; candidate ran 3.02x +/- 0.34 faster.
- Delta: paired result is 636.1 ms -> 210.3 ms, 66.9% faster on the profile-backed sort clone target.
- Validation: RCH `cargo test -p fp-columnar dense_primitive_clone -- --nocapture`, RCH `cargo check -p fp-columnar --all-targets`, RCH `cargo clippy -p fp-columnar --all-targets -- -D warnings`, local `cargo fmt -p fp-columnar -- --check`, `ubs crates/fp-columnar/src/lib.rs`, and before/after golden sha256 all passed. The existing `column_clone_preserves_values_without_copying_private_cache` test also passed, but RCH had no admissible worker and fell open locally for that one focused test.
- Score: Impact 4 x Confidence 4 / Effort 1 = 16.0, retained.
- Verdict: PRODUCTIVE

### Pass 12 - Direct Bool-Mask Row Materialization - 2026-06-03T03:46:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.7`; scope reserved for `crates/fp-columnar/src/lib.rs`, `crates/fp-frame/src/lib.rs`, `tests/artifacts/perf/*filter_bool*fi6zx_7*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: after pass 11, shifted reprofile ranked `filter_bool 100000 200` hottest at 398.5 ms mean, stddev 28.9 ms, ahead of `sort_single 100000 200` at 221.1 ms and `drop_duplicates 100000 80` at 70.9 ms. Fresh GDB sampling on `filter_bool 100000 5000` captured 63 samples; 43 stack lines included `take_rows_by_positions_unchecked`, 35 included the typed primitive gather helpers, and 34 included `Scalar` writes.
- Baseline: current post-pass-11 binary direct hyperfine `filter_bool 100000 200` mean 432.2 ms, stddev 24.7 ms, min 396.9 ms, max 466.5 ms (`tests/artifacts/perf/fp_before_filter_bool_mask_fi6zx_7.json`).
- Candidate lever: DataFrame `loc_bool` would skip positions allocation for flat indexes and gather columns directly from the checked boolean mask via a new `Column::take_bool_mask` typed-cache helper, with row MultiIndex falling back to the previous positions path.
- Isomorphism proof: golden `filter_bool 5000` sha256 stayed `eeeba6308a221e0209f4b29930d0e6f27115bff38885379d061e2a36114616d7`; the candidate preserved row order, index labels/names, column order, scalar payloads, null/NaN variants, FP bits, errors, and RNG state.
- Rebench: candidate direct hyperfine mean 461.5 ms, stddev 43.6 ms. Paired A/B in the same environment: before 414.0 ms +/- 34.2, candidate 448.9 ms +/- 36.0; before ran 1.08x +/- 0.12 faster.
- Decision: reject and back out the source lever. The extra full-mask scans per column outweighed removing the positions vector on the 50% selectivity profile workload.
- Score: below 2.0, not retained.
- Verdict: REJECTED

### Pass 13 - Direct Bool-Mask Gather Rejection Confirmation - 2026-06-03T03:57:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.6`; scope reserved for `crates/fp-columnar/src/lib.rs`, `crates/fp-frame/src/lib.rs`, `tests/artifacts/perf/*filter_mask_direct_fi6zx_6*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: post-pass-11 reprofile ranked `filter_bool 100000 200` hottest at 398.5 ms mean, ahead of `sort_single 100000 200` at 221.1 ms and `drop_duplicates 100000 80` at 70.9 ms; `gdb_filter_bool_after_sort_clone_fi6zx_5.txt` sampled scalar writes in the primitive gather path.
- Baseline: clean detached-HEAD RCH-built binary hyperfine `filter_bool 100000 200` mean 407.5 ms, stddev 37.4 ms, min 345.8 ms, max 454.9 ms (`tests/artifacts/perf/fp_before_filter_mask_direct_fi6zx_6.json`).
- Candidate lever: same direct bool-mask gather candidate as the rejected pass 12: flat-index `DataFrame::loc_bool` scans the checked mask directly and columnar primitive gather helpers scan mask+typed data instead of an intermediate positions slice.
- Isomorphism proof: golden `filter_bool 5000` sha256 stayed `eeeba6308a221e0209f4b29930d0e6f27115bff38885379d061e2a36114616d7`; row order, index labels/names, column order, scalar payloads, null/NaN variants, FP bits, errors, and RNG state are unchanged.
- Rebench: candidate direct hyperfine mean 434.7 ms, stddev 44.7 ms. Paired A/B in the same environment: baseline 403.0 ms +/- 34.3, candidate 423.6 ms +/- 40.5; baseline ran 1.05x +/- 0.13 faster.
- Decision: reject and keep no source diff. The extra full-mask scans per column still outweigh removing the positions vector on this 50% selectivity workload.
- Score: below 2.0, not retained.
- Verdict: REJECTED

### Pass 14 - Bool Positions Preallocation Probe - 2026-06-03T04:11:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.8`; scope reserved for `crates/fp-frame/src/lib.rs`, `tests/artifacts/perf/*filter_positions_prealloc_fi6zx_8*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: `filter_bool 100000 200` remains the top measured post-pass-11 workload; `gdb_filter_bool_after_sort_clone_fi6zx_5.txt` samples `DataFrame::loc_bool/iloc_bool` feeding `take_rows_by_positions_unchecked` and scalar writes in primitive gather.
- Baseline: clean RCH-built binary hyperfine `filter_bool 100000 200` mean 473.9 ms, stddev 47.3 ms, min 370.5 ms, max 520.4 ms (`tests/artifacts/perf/fp_before_filter_positions_prealloc_fi6zx_8.json`).
- Candidate lever: keep the existing positions-based gather, but count selected mask entries once and fill a preallocated `Vec<usize>` explicitly instead of collecting through `filter_map`.
- Isomorphism proof: the candidate feeds the same selected positions, in the same ascending mask order, into the same `take_rows_by_positions` path. Golden `filter_bool 5000` sha256 stayed `eeeba6308a221e0209f4b29930d0e6f27115bff38885379d061e2a36114616d7`; row order, index labels/names, column order, scalar payloads, null/NaN variants, FP bits, errors, and RNG state are unchanged.
- Rebench: candidate direct hyperfine mean 439.1 ms, stddev 33.9 ms. Paired A/B: baseline 441.5 ms +/- 34.6, candidate 416.1 ms +/- 37.1; candidate ran 1.06x +/- 0.13 faster.
- Decision: reject and keep no source diff. The apparent 6% mean win overlaps no improvement and does not meet the "real win" confidence bar.
- Remeasure: isolated RCH-built candidate binary direct hyperfine mean 425.3 ms, stddev 38.6 ms. Paired A/B in the same environment: baseline 417.0 ms +/- 30.1, candidate 432.3 ms +/- 34.9; baseline ran 1.04x +/- 0.11 faster.
- Remeasure decision: rejection reinforced; the refreshed artifacts preserve the same golden sha and keep no source diff.
- Score: below 2.0, not retained.
- Verdict: REJECTED

### Pass 15 - Named Index Constructor Probe - 2026-06-03T04:37:00-04:00
- Bead claimed: `br-frankenpandas-uza04.3`; scope reserved for `crates/fp-index/src/lib.rs`, `crates/fp-frame/src/lib.rs`, `tests/artifacts/perf/*index_name_ctor_uza04_3*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: `filter_bool 100000 200` remains the hottest measured workload after prior retained passes. `tests/artifacts/perf/gdb_filter_bool_after_sort_clone_fi6zx_5.txt` sampled `DataFrame::take_rows_by_positions_unchecked -> Index::new(labels).rename_index(self.index.name()) -> Index::set_names -> Index::clone/to_vec` while materializing filtered rows.
- Baseline: RCH-built binary hyperfine `filter_bool 100000 200` mean 421.3 ms, stddev 28.9 ms, min 372.5 ms, max 463.5 ms (`tests/artifacts/perf/fp_before_index_name_ctor_uza04_3.json`).
- Candidate lever: add `Index::new_with_name(labels, name)` and use it only in the profiled row-materialization path to avoid cloning a freshly built flat index solely to set the name.
- Isomorphism proof: the candidate produced the same labels in the same selected-mask order with the same index name and then fed identical columns through the unchanged gather path. Golden `filter_bool 5000` sha256 stayed `eeeba6308a221e0209f4b29930d0e6f27115bff38885379d061e2a36114616d7`; row order, index labels/names, column order, scalar payloads, null/NaN variants, FP bits, errors, and RNG state were unchanged.
- Rebench: candidate direct hyperfine mean 425.5 ms, stddev 43.1 ms. Paired A/B: baseline 421.6 ms +/- 35.6, candidate 432.0 ms +/- 36.1; baseline ran 1.02x +/- 0.12 faster.
- Decision: reject and keep no source diff. Avoiding this index-name clone did not move the measured filter_bool workload and fails the real-win confidence gate.
- Remeasure: clean detached-HEAD baseline was rebuilt remotely with RCH after the earlier target-dir fallback issue. Direct baseline mean 446.5 ms, stddev 25.9 ms; candidate direct mean 386.2 ms, stddev 25.1 ms. Paired A/B: baseline 431.2 ms +/- 42.7, candidate 403.9 ms +/- 44.4; candidate ran 1.07x +/- 0.16 faster.
- Remeasure decision: rejection remains because the paired confidence interval overlaps no improvement. Source diff stays absent; refreshed artifacts now point at the remote clean-baseline target.
- Score: below 2.0, not retained.
- Verdict: REJECTED

### Pass 16 - Loc Bool Unchecked Routing Probe - 2026-06-03T04:47:00-04:00
- Bead claimed: `br-frankenpandas-uza04.4`; scope reserved for `crates/fp-frame/src/lib.rs`, `tests/artifacts/perf/*loc_bool_unchecked_uza04_4*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: `filter_bool 100000 200` remains the top measured workload, and the existing GDB stack samples route through `DataFrame::loc_bool -> take_rows_by_positions -> take_rows_by_positions_unchecked`.
- Baseline: RCH-built current-source binary hyperfine `filter_bool 100000 200` mean 435.2 ms, stddev 33.3 ms, min 394.7 ms, max 485.6 ms (`tests/artifacts/perf/fp_before_loc_bool_unchecked_uza04_4.json`).
- Candidate lever: because `loc_bool` first validates `mask.len() == self.len()` and builds every selected position from `mask.enumerate()`, call `take_rows_by_positions_unchecked` directly and skip the redundant position-bounds validation loop.
- Isomorphism proof: the candidate constructs the exact same selected positions in ascending mask order and feeds them into the same unchecked materializer used after validation. Golden `filter_bool 5000` sha256 stayed `eeeba6308a221e0209f4b29930d0e6f27115bff38885379d061e2a36114616d7`; row order, index labels/names, column order, scalar payloads, null/NaN variants, FP bits, errors, and RNG state were unchanged.
- Rebench: candidate direct hyperfine mean 444.0 ms, stddev 30.5 ms. Paired A/B: baseline 425.0 ms +/- 39.4, candidate 543.9 ms +/- 108.9; baseline ran 1.28x +/- 0.28 faster.
- Decision: reject and keep no source diff. Removing this validation loop did not improve the workload and produced a noisy regression.
- Score: below 2.0, not retained.
- Verdict: REJECTED

### Pass 17 - Typed Gather Iterator Collect Probe - 2026-06-03T05:05:00-04:00
- Bead claimed: `br-frankenpandas-uza04.5`; scope reserved for `crates/fp-columnar/src/lib.rs`, `tests/artifacts/perf/*typed_gather_collect_uza04_5*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: `filter_bool 100000 200` remains the active row-gather target, and prior GDB samples stop in `Column::take_cached_all_valid_primitive_positions` while materializing scalar values.
- Baseline: RCH-built current-source binary hyperfine `filter_bool 100000 200` mean 436.1 ms, stddev 29.1 ms, min 405.6 ms, max 494.0 ms (`tests/artifacts/perf/fp_before_typed_gather_collect_uza04_5.json`).
- Candidate lever: change the cached all-valid primitive gather branches from manual `Vec::with_capacity` plus `push` loops to iterator `map(...).collect()` for Bool, Int64, Float64, Timedelta64, Datetime64, and Period.
- Isomorphism proof: the candidate reads the same `positions` slice in order, indexes the same typed backing buffers, wraps each copied primitive in the same `Scalar` variant, and leaves fallback/error paths unchanged. Golden `filter_bool 5000` sha256 stayed `eeeba6308a221e0209f4b29930d0e6f27115bff38885379d061e2a36114616d7`; row order, labels/names, column order, scalar payloads, null/NaN variants, FP bits, errors, and RNG were unchanged.
- Rebench: candidate direct hyperfine mean 457.6 ms, stddev 72.2 ms. Paired A/B: baseline 506.9 ms +/- 47.5, candidate 440.8 ms +/- 71.9; candidate ran 1.15x +/- 0.22 faster.
- Decision: reject and keep no source diff. The paired interval overlaps no improvement, and standalone candidate timing was slower/noisier than the baseline.
- Score: below 2.0, not retained.
- Verdict: REJECTED

### Pass 18 - Lazy Float64 Gather Scalar Materialization - 2026-06-03T05:58:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.9`; scope reserved for `crates/fp-columnar/src/lib.rs`, `tests/artifacts/perf/*lazy_float_gather_fi6zx_9*`, `tests/artifacts/perf/*lazy_float64_gather_fi6zx_9*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: `filter_bool 100000 200` remained the active top row-gather workload after the prior retained clone-cache win. Existing GDB evidence sampled `DataFrame::take_rows_by_positions_unchecked -> Column::take_cached_all_valid_primitive_positions` and Scalar writes/drops; direct bool-mask gather, positions preallocation, index-name construction, unchecked loc_bool routing, and iterator-collect gather were all rejected below the real-win confidence gate.
- Baseline: clean remote RCH-built before binary hyperfine `filter_bool 100000 200` mean 410.5 ms, stddev 40.7 ms (`tests/artifacts/perf/fp_before_lazy_float64_gather_fi6zx_9_clean_remote.json`); paired A/B baseline in the same environment was 428.4 ms +/- 38.0.
- Lever: for all-valid Float64 `Column::take_positions`, gather a contiguous `Vec<f64>` and store it in lazy scalar storage backed by `OnceLock<Vec<Scalar>>`, so the hot row-filter path avoids immediate public `Vec<Scalar>` materialization. Other dtypes, null-bearing columns, serialization, equality, debug, and public `values()` observation still materialize the same scalar sequence.
- Isomorphism proof: the fast path reads the same `positions` slice in order, copies the same Float64 payloads, preserves signed-zero and NaN bits until public scalar materialization, and keeps the same all-valid validity mask. Output row order, index labels/names, column order, tie-breaking, null policy, error classes, RNG state, and fallback paths are unchanged; the new focused test asserts exact gathered Float64 bit patterns before materialization and equality after `values()` materializes.
- Golden-output proof: `perf_profile golden filter_bool 5000` sha256 stayed `eeeba6308a221e0209f4b29930d0e6f27115bff38885379d061e2a36114616d7`; before/after golden files compare equal.
- Rebench: RCH-built candidate direct hyperfine `filter_bool 100000 200` mean 233.7 ms, stddev 10.4 ms (`tests/artifacts/perf/fp_after_lazy_float64_gather_fi6zx_9.json`). Paired A/B: baseline 428.4 ms +/- 38.0, candidate 232.6 ms +/- 10.1; candidate ran 1.84x +/- 0.18 faster.
- Delta: paired result is 428.4 ms -> 232.6 ms, 45.7% faster on the profile-backed filter_bool gather target.
- Validation: RCH `cargo test -p fp-columnar float64_take_positions_defers_scalar_materialization -- --nocapture`, RCH `cargo test -p fp-columnar take_positions -- --nocapture`, RCH `cargo check -p fp-columnar --all-targets`, RCH `cargo clippy -p fp-columnar --all-targets -- -D warnings`, local `cargo fmt -p fp-columnar -- --check`, `ubs crates/fp-columnar/src/lib.rs`, and before/after golden sha256 all passed. UBS reported existing warning inventories but no critical failures and its fmt/clippy/build subchecks passed.
- Score: Impact 4 x Confidence 4 / Effort 2 = 8.0, retained.
- Verdict: PRODUCTIVE

### Pass 19 - Lazy Float64 Clone Scalar Materialization - 2026-06-03T06:23:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.10`; scope reserved for `crates/fp-columnar/src/lib.rs`, `tests/artifacts/perf/*lazy_float64_clone_fi6zx_10*`, `tests/artifacts/perf/fp_reprofile_after_lazy_float64_gather_fi6zx_9.json`, `tests/artifacts/perf/gdb_sort_single_after_lazy_float64_fi6zx_9.txt`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: shifted RCH hyperfine after pass 18 ranked `sort_single 100000 200` hottest at 270.6 ms +/- 19.6, ahead of `filter_bool` 235.2 ms and `drop_duplicates` 76.5 ms (`tests/artifacts/perf/fp_reprofile_after_lazy_float64_gather_fi6zx_9.json`). Fresh GDB sampling on `sort_single 100000 5000` captured 40 samples: Scalar drops 39, `ScalarValues` drops 14, BTreeMap column drops 28, `clone_dense_values_from_cache` 5, `sort_values_na` 9, and IndexLabel `to_vec` 7 (`tests/artifacts/perf/gdb_sort_single_after_lazy_float64_fi6zx_9.txt`).
- Baseline: current RCH-built before binary hyperfine `sort_single 100000 200` mean 244.7 ms, stddev 30.5 ms (`tests/artifacts/perf/fp_before_lazy_float64_clone_fi6zx_10.json`); paired A/B baseline in the same environment was 246.9 ms +/- 21.9.
- Lever: `Column::clone` still drops the private typed cache, but when all-valid Float64 public values can be reconstructed from private `ColumnData::Float64`, the clone now stores `ScalarValues::LazyAllValidFloat64` instead of eagerly collecting `Vec<Scalar>`. Other primitive dtypes keep the previous eager scalar-vector reconstruction, and missing/null-bearing or mismatched cache states still fall back to the old clone path.
- Isomorphism proof: the cloned public dtype, length, validity mask, and scalar sequence are unchanged; public `values()`, serde, debug, and equality materialize the same Float64 scalar payloads on observation. Output row order, index labels/names, column order, sort tie policy, null/NaN policy, Float64 bits including signed zero, error classes, private-cache-dropping behavior, and RNG state are unchanged. The focused test asserts the cloned Float64 data bits before materialization, verifies the `OnceLock` is empty before observation, and verifies equality plus materialization after `values()`.
- Golden-output proof: `perf_profile golden sort_single 5000` sha256 stayed `0d34cbf3261960c869ee85a7a91641c038e9e2f7c253ca2dec701075da028493`; before/after golden files compare equal.
- Rebench: RCH-built candidate direct hyperfine `sort_single 100000 200` mean 72.1 ms, stddev 4.5 ms (`tests/artifacts/perf/fp_after_lazy_float64_clone_fi6zx_10.json`). Paired A/B: baseline 246.9 ms +/- 21.9, candidate 72.5 ms +/- 3.9; candidate ran 3.41x +/- 0.35 faster.
- Delta: paired result is 246.9 ms -> 72.5 ms, 70.6% faster on the profile-backed sorted sort_values clone target.
- Validation: RCH `cargo test -p fp-columnar dense_primitive_clone --lib -- --nocapture`, RCH `cargo check -p fp-columnar --all-targets`, RCH `cargo clippy -p fp-columnar --all-targets -- -D warnings`, local `cargo fmt -p fp-columnar -- --check`, `ubs crates/fp-columnar/src/lib.rs`, and before/after golden sha256 all passed. UBS reported existing warning inventories but no critical failures.
- Score: Impact 4 x Confidence 4 / Effort 2 = 8.0, retained.
- Verdict: PRODUCTIVE

### Post-Pass 19 Shifted-Bottleneck Reprofile - 2026-06-03T06:31:00-04:00
- Artifact: `tests/artifacts/perf/fp_reprofile_after_lazy_float64_clone_fi6zx_10.json`.
- Binary: RCH-built pass-19 candidate `/data/tmp/cargo-target-rubygoose-fi6zx10-candidate/release-perf/examples/perf_profile`, avoiding the dirty in-progress `fp-join` worktree.
- Hyperfine rank:
  - `join_1to1 100000 50`: 1.667 s +/- 0.063.
  - `filter_bool 100000 200`: 246.0 ms +/- 9.1.
  - `sort_single 100000 200`: 73.4 ms +/- 7.4.
  - `drop_duplicates 100000 80`: 66.1 ms +/- 1.8.
- Decision: no ready `[perf]` bead exists after `.10`; `br-frankenpandas-typed-columnar-storage-epic-fi6zx.11` is already in progress on the join hotspot, and `crates/fp-join/src/lib.rs` has peer edits, so do not collide.

### Pass 20 - Inline One-Match Join Probe Buckets - 2026-06-03T06:36:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.11`; scope reserved for `crates/fp-join/src/lib.rs`, `tests/artifacts/perf/*join_smallvec_probe_fi6zx_11*`, `tests/artifacts/perf/fp_reprofile_after_lazy_float64_gather_fi6zx_9.json`, `tests/artifacts/perf/gdb_join_1to1_after_lazy_fi6zx9.txt`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: RCH reprofile after pass 18 ranked `join_1to1 100000 50` slowest at 1.553 s total, and clean pass-19 shifted reprofile ranked it slowest again at 1.667 s +/- 0.063. GDB stack sampling on `join_1to1 100000 5000` hit `merge_dataframes_on_with_options` right-map probe-bucket growth at `crates/fp-join/src/lib.rs:1057` and map/drop work at line 1313.
- Baseline: clean `HEAD d9a80c4e` RCH-built before binary direct hyperfine `join_1to1 100000 50` mean 1.609 s, stddev 77.1 ms; paired A/B baseline in the same environment was 1.555 s +/- 53.6 ms.
- Lever: replace `FxHashMap<&CompositeJoinKey, Vec<usize>>` probe buckets in both join-side maps with `SmallVec<[usize; 1]>`, eliminating one heap allocation for the common one-match bucket while preserving spill behavior for duplicate keys. Scanner-only equality syntax and a test `contains_key` lint were adjusted without touching the hot path semantics.
- Isomorphism proof: keys, hash/equality, map entry selection, left/right row probe order, duplicate insertion order, many-to-many spill order, output row order, index labels, column order, suffix handling, error classes, tie-breaking, floating-point behavior, and RNG state are unchanged. `SmallVec<[usize; 1]>` iterates the same pushed positions as `Vec<usize>` and spills for duplicate matches instead of changing multiplicity.
- Golden-output proof: clean-head `perf_profile golden join_1to1 5000` before/after sha256 stayed `18988b22befdb71941112638f6f1c3415cbf0f79fc1f00ecd013318d924682c7`; before/after golden files compare equal and `sha256sum -c` passed for both.
- Rebench: clean-head RCH-built candidate direct hyperfine mean 533.4 ms, stddev 44.7 ms. Paired A/B: baseline 1.555 s +/- 53.6 ms, candidate 531.6 ms +/- 48.2 ms; candidate ran 2.93x +/- 0.28 faster.
- Delta: paired result is 1.555 s -> 531.6 ms, 65.8% faster on the profile-backed one-to-one join target.
- Validation: clean candidate RCH `cargo test -p fp-join --lib merge -- --nocapture`, RCH `cargo check -p fp-join --all-targets`, RCH `cargo clippy -p fp-join --all-targets -- -D warnings`, local `cargo fmt -p fp-join -- --check`, `ubs crates/fp-join/src/lib.rs`, and before/after golden sha256 all passed. UBS reported existing warning inventories but no critical failures.
- Score: Impact 5 x Confidence 5 / Effort 2 = 12.5, retained.
- Verdict: PRODUCTIVE

### Post-Pass 20 Shifted-Bottleneck Reprofile - 2026-06-03T06:40:00-04:00
- Artifact: `tests/artifacts/perf/fp_reprofile_after_join_smallvec_probe_fi6zx_11.json`.
- Binary: RCH-built clean `ff24831f` worktree `/data/tmp/cargo-target-rubygoose-fi6zx11-postreprofile/release-perf/examples/perf_profile`, avoiding the shared dirty main checkout.
- Hyperfine rank:
  - `series_add_align 100000 100`: 2.223 s +/- 0.048.
  - `join_1to1 100000 50`: 494.3 ms +/- 24.2.
  - `filter_bool 100000 200`: 248.6 ms +/- 16.5.
  - `sort_single 100000 200`: 77.6 ms +/- 4.0.
  - `drop_duplicates 100000 80`: 75.1 ms +/- 7.7.
- Decision: pass 20 moved the join bottleneck down; the next profile-backed target is `series_add_align`.

### Pass 21 - Cached Series Alignment Semantic Fingerprints - 2026-06-03T08:58:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.12`; scope reserved for `crates/fp-index/src/lib.rs`, `crates/fp-frame/src/lib.rs`, `crates/fp-columnar/src/lib.rs`, `tests/artifacts/perf/*series_add_align_fi6zx_12*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: post-pass-20 RCH reprofile ranked `series_add_align 100000 100` hottest at 2.223 s +/- 0.048, and GDB sampling of `series_add_align 100000 1000` attributed the hot stack to semantic witness fingerprinting (`semantic_index_identity` / serde_json / sha2 / `record_alignment_semantic_witness`) far more often than the already-typed arithmetic path.
- Baseline: coherent no-cache ref `50bc2bc7` RCH-built direct hyperfine `series_add_align 100000 100` mean 2.256 s, stddev 0.032 (`tests/artifacts/perf/fp_before_series_add_align_fi6zx_12_ref50bc2bc7.json`); paired A/B baseline in the same environment was 2.212 s +/- 0.025.
- Lever: add a runtime-only `OnceLock<String>` to `Index` for the exact labels-derived AACE semantic fingerprint, exposed through `semantic_labels_fingerprint_with`. The cache is skipped by serde, ignored by equality, and does not include index names, matching the existing witness identity contract.
- Isomorphism proof: labels, label ordering, duplicate state, index names, alignment order, output values, null/NaN propagation, floating-point bits, tie-breaking, error classes, and RNG state are unchanged. The first fingerprint computation still uses the same serde_json label payload and `fp_runtime::semantic_fingerprint_bytes`; later calls reuse the identical string. Focused cache coverage proves the compute closure runs once and subsequent calls return the same cached value.
- Golden-output proof: `perf_profile golden series_add_align 5000` before/after sha256 stayed `0d328c9236e35ac3f85c109248a2177080d25ac7693f01873a4dc0b97d219ca2`; before/after golden files compare equal.
- Rebench: RCH-built candidate direct hyperfine `series_add_align 100000 100` mean 1.359 s, stddev 0.024 (`tests/artifacts/perf/fp_after_series_add_align_fi6zx_12.json`). Paired A/B: baseline 2.212 s +/- 0.025, candidate 1.386 s +/- 0.054; candidate ran 1.60x +/- 0.06 faster.
- Delta: paired result is 2.212 s -> 1.386 s, 37.3% faster on the profile-backed shifted Series alignment target.
- Validation: RCH `cargo test -p fp-index semantic_fingerprint_cache_reuses_label_result --lib -- --nocapture`, RCH `cargo test -p fp-frame series_add_aligns_on_union_index --lib -- --nocapture`, RCH `cargo test -p fp-frame series_add_emits_alignment_semantic_witness_tn6qb3 --lib -- --nocapture`, RCH `cargo check -p fp-index --all-targets`, RCH `cargo check -p fp-frame --all-targets`, RCH `cargo clippy -p fp-index --all-targets -- -D warnings`, RCH `cargo clippy -p fp-frame --all-targets -- -D warnings`, local `cargo fmt -p fp-index -- --check`, and before/after golden sha256 all passed. `ubs crates/fp-index/src/lib.rs` completed but returned non-zero on the file's existing panic/unwrap/direct-indexing inventory; its fmt/clippy/build subchecks passed and the new cache line only appeared in generic clone inventory.
- Score: Impact 4 x Confidence 5 / Effort 2 = 10.0, retained.
- Verdict: PRODUCTIVE

### Post-Pass 21 Shifted-Bottleneck Reprofile - 2026-06-03T09:13:00-04:00
- Artifact: `tests/artifacts/perf/fp_reprofile_after_semantic_fingerprint_cache_fi6zx_12.json`.
- Binary: RCH-built clean `4f80db2d` worktree `/data/projects/frankenpandas-fi6zx12-postreprofile`, retrieved at `/data/tmp/cargo-target-rubygoose-fi6zx12-postreprofile-rch/release-perf/examples/perf_profile`. A first `/data/tmp` worktree attempt fell back to local build due RCH path normalization and was discarded as evidence.
- Hyperfine rank:
  - `series_add_align 100000 100`: 946.5 ms +/- 31.3.
  - `join_1to1 100000 50`: 557.2 ms +/- 30.6.
  - `filter_bool 100000 200`: 230.4 ms +/- 9.2.
  - `sort_single 100000 200`: 73.5 ms +/- 4.9.
  - `drop_duplicates 100000 80`: 69.4 ms +/- 10.4.
- Decision: pass 21 removed a large witness-fingerprint tax but `series_add_align` remains the top profile-backed target. Current dirty worktree already contains in-progress peer bead `br-frankenpandas-typed-columnar-storage-epic-fi6zx.13` on streaming integer-label witness fingerprints, so the next optimization pass should coordinate around that bead rather than collide.

### Pass 22 - Stream Integer-Label Semantic Witness Fingerprints - 2026-06-03T09:18:00-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.13`; scope reserved for `crates/fp-frame/src/lib.rs`, `crates/fp-runtime/src/lib.rs`, `tests/artifacts/perf/*series_add_align_fi6zx_13*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: current pass-13 reprofile ranked `series_add_align 100000 100` hottest at 2.385 s +/- 0.062 (`tests/artifacts/perf/fp_reprofile_current_series_add_align_fi6zx_12.json`), and GDB stack sampling on `series_add_align 100000 1000` hit `semantic_index_identity` and `record_alignment_semantic_witness` 28 samples each, with 336 `serde_json` frames (`tests/artifacts/perf/gdb_series_add_align_current_fi6zx_12.txt`). The perf harness builds Int64 shifted indexes, so integer-label witness fingerprinting is a profile-backed target.
- Baseline: RCH-built before binary direct hyperfine `series_add_align 100000 100` mean 2.277 s, stddev 0.030 (`tests/artifacts/perf/fp_before_series_add_align_fi6zx_13.json`).
- Lever: add `fp_runtime::SemanticFingerprintBuilder` for incremental sha256 updates, then use it in `fp-frame` to stream the exact serde_json-compatible bytes for `Int64`, `Timedelta64`, and `Datetime64` index-label arrays. `Utf8` and mixed string indexes still fall back to the previous `serde_json::to_vec` path.
- Isomorphism proof: the streamed byte grammar matches serde's tagged enum JSON for integer-like labels exactly, including commas, field names, signed decimal values, and `i64::MIN`; the new unit test compares the streamed sha256 to `serde_json::to_vec` + `semantic_fingerprint_bytes` and proves string labels do not enter the fast path. Label ordering, duplicate flags, index names, alignment order, output values, null/NaN propagation, floating-point bits, tie-breaking, error classes, and RNG state are unchanged.
- Golden-output proof: `perf_profile golden series_add_align 5000` before/after sha256 stayed `0d328c9236e35ac3f85c109248a2177080d25ac7693f01873a4dc0b97d219ca2`; before/after golden files compare byte-for-byte equal.
- Rebench: RCH-built candidate direct hyperfine `series_add_align 100000 100` mean 1.375 s, stddev 0.029 (`tests/artifacts/perf/fp_after_series_add_align_fi6zx_13.json`).
- Delta: direct result is 2.277 s -> 1.375 s, 39.6% faster, 1.66x speedup on the profile-backed shifted Series alignment target.
- Validation: RCH `cargo test -p fp-frame tests::semantic_integer_label_fingerprint_matches_serde_json -- --exact`, RCH `cargo test -p fp-frame tests::series_add_emits_alignment_semantic_witness_tn6qb3 -- --exact`, RCH `cargo check -p fp-frame -p fp-runtime --all-targets`, RCH `cargo clippy -p fp-frame -p fp-runtime --all-targets -- -D warnings`, local `rustfmt --check crates/fp-runtime/src/lib.rs crates/fp-frame/src/lib.rs`, and before/after golden sha256 all passed. `ubs crates/fp-runtime/src/lib.rs` completed with existing warning inventory and no critical issues; `ubs crates/fp-frame/src/lib.rs` produced no findings before a 120s timeout on the large file. A stale RCH worker source copy initially failed `fp-io` with old Bool dereferences; warming the RCH cache refreshed workers and the release-perf build then passed.
- Score: Impact 4 x Confidence 5 / Effort 2 = 10.0, retained.
- Verdict: PRODUCTIVE

### Post-Pass 22 Shifted-Bottleneck Reprofile - 2026-06-03T09:19:00-04:00
- Artifact: `tests/artifacts/perf/fp_reprofile_after_series_add_align_fi6zx_13.json`.
- Binary: RCH-built pass-22 candidate `/data/tmp/cargo-target-rubygoose-fi6zx13-after/release-perf/examples/perf_profile`.
- Hyperfine rank:
  - `series_add_align 100000 100`: 1.371 s +/- 0.014.
  - `join_1to1 100000 50`: 562.6 ms +/- 44.5.
  - `filter_bool 100000 200`: 250.1 ms +/- 6.5.
  - `sort_single 100000 200`: 73.2 ms +/- 2.2.
  - `drop_duplicates 100000 80`: 63.4 ms +/- 6.4.
- Decision: pass 22 removed the integer-label serde allocation tax, but `series_add_align` remains the top profile-backed target; next pass should sample the residual series alignment path again because the bottleneck has shifted within that scenario.

### Pass 23 - Fuse Aligned Float64 Series Arithmetic - 2026-06-03T09:46:33-04:00
- Bead claimed: `br-frankenpandas-typed-columnar-storage-epic-fi6zx.14`; scope reserved for `crates/fp-columnar/src/lib.rs`, `crates/fp-frame/src/lib.rs`, `tests/artifacts/perf/*series_add_align_fi6zx_14*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: post-pass-22 reprofile ranked `series_add_align 100000 100` hottest at 1.371 s +/- 0.014, and fresh GDB sampling on the pass-22 binary hit `aligned_binary_f64` / `binary_op_with_policy` / `align_union_sorted_unique`, with visible allocation-zeroing under the temporary left/right aligned buffers (`tests/artifacts/perf/gdb_series_add_align_after_stream_fi6zx_14.txt`).
- Baseline: RCH-built pass-22 before binary direct hyperfine `series_add_align 100000 100` mean 1.348 s, stddev 0.025 (`tests/artifacts/perf/fp_before_series_add_align_fi6zx_14.json`).
- Lever: fuse `Column::aligned_binary_f64` so it emits the Float64 output scalar stream directly from left/right position maps, eliminating the temporary gathered `Vec<f64>` left/right buffers, two validity masks, and the intermediate result-data vector for this already-aligned Float64 path. The generic `vectorized_binary_f64` path and the fused path share the same `binary_f64_apply` operation selector.
- Isomorphism proof: left/right position traversal remains in union-index order, missing slots and nan-aware invalid inputs still emit `Null(NaN)`, valid inputs use the same f64 operation function for add/sub/mul/div/mod/pow/floor-div, Python-style mod/floor-div helpers are unchanged, output dtype is still Float64, row/index/column ordering and duplicate/tie behavior are untouched, no RNG state exists in this path, and floating-point arithmetic order is still exactly one scalar operation per aligned pair.
- Golden-output proof: `perf_profile golden series_add_align 5000` before/after sha256 stayed `0d328c9236e35ac3f85c109248a2177080d25ac7693f01873a4dc0b97d219ca2`; before/after golden files compare byte-for-byte equal.
- Rebench: RCH-built candidate direct hyperfine `series_add_align 100000 100` mean 1.114 s, stddev 0.031 (`tests/artifacts/perf/fp_after_series_add_align_fi6zx_14.json`).
- Delta: direct result is 1.348 s -> 1.114 s, 17.4% faster, 1.21x speedup on the profile-backed shifted Series alignment target.
- Validation: RCH `cargo test -p fp-columnar aligned_binary_f64_matches_reindex_then_binary_numeric --lib -- --nocapture`, RCH `cargo check -p fp-columnar --all-targets`, RCH `cargo clippy -p fp-columnar --all-targets -- -D warnings`, RCH `cargo check -p fp-frame --all-targets`, RCH `cargo clippy -p fp-frame --all-targets -- -D warnings`, RCH `cargo test -p fp-frame series_add --lib -- --nocapture`, local `cargo fmt -p fp-columnar -- --check`, `ubs crates/fp-columnar/src/lib.rs`, and before/after golden sha256 all passed. UBS reported the file's existing warning inventory but no critical findings; its fmt/clippy/build subchecks were clean.
- Score: Impact 3 x Confidence 5 / Effort 2 = 7.5, retained.
- Verdict: PRODUCTIVE

### Post-Pass 23 Shifted-Bottleneck Reprofile - 2026-06-03T09:46:33-04:00
- Artifact: `tests/artifacts/perf/fp_reprofile_after_series_add_align_fi6zx_14.json`.
- Binary: RCH-built pass-23 candidate `/data/tmp/cargo-target-rubygoose-fi6zx14-after/release-perf/examples/perf_profile`.
- Hyperfine rank:
  - `series_add_align 100000 100`: 1.116 s +/- 0.018.
  - `join_1to1 100000 50`: 539.2 ms +/- 49.9.
  - `filter_bool 100000 200`: 231.6 ms +/- 6.6.
  - `sort_single 100000 200`: 69.5 ms +/- 3.7.
  - `drop_duplicates 100000 80`: 62.6 ms +/- 2.6.
- Decision: pass 23 removed the aligned Float64 temporary-buffer tax, but `series_add_align` remains the top profile-backed target; next pass should profile the residual frame/index alignment path again before selecting a lever.

### Pass 24 - Typed Numeric CSV Column Construction - 2026-06-03T12:50:00-04:00
- Bead claimed: `br-frankenpandas-w4vff`; scope reserved for `crates/fp-columnar/src/lib.rs`, `crates/fp-io/src/lib.rs`, `tests/artifacts/perf/*csv_read*w4vff*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: bead `br-frankenpandas-w4vff` carried the profile conclusion that `read_csv` was dominated by AoS `Vec<Scalar>` construction/inference/coercion after the standalone fast-float parser lever was rejected. The retained primitive is the SoA typed-column construction step: parse all-valid numeric CSV columns straight into typed vectors and hand those to `ColumnData`, avoiding scalar AoS materialization until observation.
- Baseline: stored RCH-built before binary hyperfine `csv_read 100000 20` mean 1.45674832966 s, stddev 0.03045057276 (`tests/artifacts/perf/fp_before_csv_read.json`). The paired stored rerun of the prior code path was 1.43076961431 s, stddev 0.02262515118 (`tests/artifacts/perf/fp_after_csv_read_rerun.json`).
- Lever: add lazy all-valid Int64 scalar storage plus public `Column::from_i64_values` and `Column::from_f64_values` constructors, then add a default-CSV numeric probe that builds one typed vector per column. The probe only retains all-valid Int64/Float64 semantics; it bails out to the existing parser for NA markers, empty fields, NaN spellings, bool-like tokens, object/raw-text preservation cases, duplicate headers, zero-row files, and CSV shape errors.
- Isomorphism proof: column order, row index order, duplicate-header errors, ragged-row errors, object fallback, bool fallback, pandas-default NA/null handling, NaN-invalid semantics, signed-zero bits, infinities, integer-to-float promotion order, output dtypes, output scalar observations, tie-breaking, floating-point arithmetic, and RNG state are unchanged. The fast path only returns a frame when every parsed field has already proven all-valid numeric default-CSV semantics; otherwise the old scalar path remains authoritative.
- Golden-output proof: `perf_profile golden csv_read 5000` bead-specific before/after sha256 stayed `17f95925cec162c66933d5e935566b6f6fec5f8ef56872b98b00fb97ebf59fff`; `tests/artifacts/perf/golden_before_csv_read_w4vff.txt` and `tests/artifacts/perf/golden_after_csv_read_w4vff_current.txt` compare byte-for-byte equal.
- Rebench: RCH-built current binary direct hyperfine `csv_read 100000 20` mean 0.99407312684 s, stddev 0.01625099803 (`tests/artifacts/perf/fp_after_csv_read_w4vff_current.json`).
- Delta: stored before 1.45674832966 s -> current 0.99407312684 s, 31.8% faster, 1.47x speedup on the profile-backed CSV read target.
- Validation: RCH `cargo test -p fp-columnar typed_all_valid_constructors_seed_dense_storage --lib -- --nocapture`, RCH `cargo test -p fp-io read_csv_typed_numeric --lib -- --nocapture`, RCH `cargo check -p fp-columnar -p fp-io --all-targets`, RCH `cargo clippy -p fp-columnar -p fp-io --all-targets -- -D warnings`, local `cargo fmt -p fp-io -p fp-columnar -- --check`, `ubs crates/fp-columnar/src/lib.rs crates/fp-io/src/lib.rs`, RCH release-perf build of `fp-conformance` example `perf_profile`, hyperfine, and before/after golden sha256 all passed. UBS reported existing warning inventories but no critical failures.
- Score: Impact 4 x Confidence 4 / Effort 2 = 8.0, retained.
- Verdict: PRODUCTIVE

### Post-Pass 24 Shifted-Bottleneck Reprofile - 2026-06-03T12:50:00-04:00
- Artifact: `tests/artifacts/perf/fp_reprofile_after_csv_typed_w4vff.json`.
- Binary: RCH-built current candidate `/data/tmp/cargo-target-rubygoose-w4vff-current/release-perf/examples/perf_profile`.
- Hyperfine rank:
  - `csv_read 100000 20`: 1.007 s +/- 0.009.
  - `series_add_align 100000 100`: 969.5 ms +/- 21.9.
  - `join_1to1 100000 50`: 594.9 ms +/- 49.2.
  - `filter_bool 100000 200`: 242.8 ms +/- 6.3.
  - `sort_single 100000 200`: 73.6 ms +/- 5.3.
  - `drop_duplicates 100000 80`: 67.0 ms +/- 1.4.
- Decision: pass 24 moved CSV read from 1.46 s into parity with the residual top path, but `csv_read` remains narrowly hottest. The next retained pass should profile the residual CSV path and attack the next algorithmic primitive rather than micro-tuning the current loop.

### Pass 25 - Byte-Record CSV Numeric Fast Path - 2026-06-03T13:28:33-04:00
- Bead claimed: `br-frankenpandas-w8826`; scope reserved for `crates/fp-io/src/lib.rs`, `crates/fp-columnar/src/lib.rs`, `tests/artifacts/perf/*w8826*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: post-pass-24 reprofile ranked `csv_read 100000 20` narrowly hottest at 1.007 s +/- 0.009, and fresh GDB fallback sampling hit `try_read_csv_str_typed_numeric` in 38/40 samples with `read_record_dfa`/`scan_and_copy`/`__memcpy_avx_unaligned_erms`, string record output, and numeric parse frames in the residual path (`tests/artifacts/perf/gdb_csv_read_w8826_current.txt`).
- Alien primitive: zero-copy byte framing plus branch-light numeric token scanning from the alien-graveyard parser/vectorized-batch family; replace decoded `StringRecord` hot-loop work with byte-slice framing while keeping the old scalar parser as the compatibility authority.
- Baseline: bead before RCH-built binary direct hyperfine `csv_read 100000 20` mean 0.98718224774 s, stddev 0.01885253715 (`tests/artifacts/perf/fp_before_csv_read_w8826.json`).
- Lever: switch the all-valid numeric CSV probe from `reader.records()`/`StringRecord` to `reader.byte_records()`/byte slices, add byte NA/bool/ASCII-trim guards, parse integers from ASCII bytes, and parse floats with `fast-float2` on the byte slice before falling back to Rust's string parser for accepted finite spellings.
- Isomorphism proof: CSV row/header order, duplicate-header handling, ragged-row errors, all-valid-only retention, fallback object/raw-text behavior, pandas-default NA handling, NaN-invalid semantics, bool fallback, signed-zero bits, infinities, integer-to-float promotion order, output dtypes, scalar observation order, floating-point arithmetic, tie-breaking, and RNG state are unchanged. Non-ASCII numeric-looking fields fail the byte fast path and re-enter the old parser rather than changing Unicode trim/coercion behavior.
- Golden-output proof: `perf_profile golden csv_read 5000` before/after sha256 stayed `17f95925cec162c66933d5e935566b6f6fec5f8ef56872b98b00fb97ebf59fff`; `tests/artifacts/perf/golden_before_csv_read_w8826.txt` and `tests/artifacts/perf/golden_after_csv_read_w8826.txt` compare byte-for-byte equal.
- Rebench: RCH-built candidate direct hyperfine `csv_read 100000 20` mean 0.95300115956 s, stddev 0.01154684951 (`tests/artifacts/perf/fp_after_csv_read_w8826.json`); interleaved A/B confirmed old mean 1.00239221810 s vs candidate mean 0.93152042230 s, 1.08x faster (`tests/artifacts/perf/fp_ab_csv_read_w8826.json`).
- Delta: direct result is 0.98718224774 s -> 0.95300115956 s, 3.5% faster; paired A/B result is 1.00239221810 s -> 0.93152042230 s, 7.1% faster, 1.08x speedup on the profile-backed CSV read target.
- Validation: RCH `cargo test -p fp-io read_csv_typed_numeric --lib -- --nocapture`, RCH `cargo test -p fp-io read_csv_simple_typed_numeric --lib -- --nocapture`, RCH `cargo check -p fp-io --all-targets`, RCH `cargo clippy -p fp-io --all-targets -- -D warnings`, local `cargo fmt -p fp-io -- --check`, `ubs crates/fp-io/src/lib.rs`, RCH release-perf build of `fp-conformance` example `perf_profile`, hyperfine, paired A/B, and before/after golden sha256 all passed. UBS reported the file's existing warning inventory but no critical failures.
- Score: Impact 3 x Confidence 4 / Effort 2 = 6.0, retained.
- Verdict: PRODUCTIVE

### Post-Pass 25 Shifted-Bottleneck Reprofile - 2026-06-03T13:28:33-04:00
- Artifact: `tests/artifacts/perf/fp_reprofile_after_csv_byte_w8826.json`.
- Binary: RCH-built pass-25 candidate `/data/tmp/cargo-target-rubygoose-w8826-current/release-perf/examples/perf_profile`.
- Hyperfine rank:
  - `series_add_align 100000 100`: 1.005 s +/- 0.010.
  - `csv_read 100000 20`: 984.7 ms +/- 9.9.
  - `join_1to1 100000 50`: 526.1 ms +/- 27.1.
  - `filter_bool 100000 200`: 243.5 ms +/- 4.2.
  - `sort_single 100000 200`: 73.8 ms +/- 2.7.
  - `drop_duplicates 100000 80`: 65.7 ms +/- 3.7.
- Decision: pass 25 removed a residual CSV string-decoding tax, and the next profile-backed target shifts back to `series_add_align`. The next pass should sample the residual series alignment path again and harvest a deeper structural primitive rather than micro-tuning CSV further.

### Pass 26 - Reuse Typed Float64 Buffers In Aligned Series Arithmetic - 2026-06-03T13:57:00-04:00
- Bead claimed: `br-frankenpandas-j5gqz`; scope reserved for `crates/fp-columnar/src/lib.rs`, `crates/fp-frame/src/lib.rs`, `crates/fp-runtime/src/lib.rs`, `tests/artifacts/perf/*j5gqz*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: post-pass-25 reprofile ranked `series_add_align 100000 100` hottest at 1.005 s +/- 0.010, and GDB fallback sampling on `series_add_align 100000 1000` hit `ColumnData::from_scalars`, `aligned_binary_f64`, `nan_aware_validity`, scalar writes, and alignment position/fingerprint work in the residual path (`tests/artifacts/perf/gdb_series_add_align_j5gqz_current.txt`).
- Baseline: RCH-built pass-25 before binary direct hyperfine `series_add_align 100000 100` mean 952.5 ms, stddev 26.0 ms (`tests/artifacts/perf/fp_before_series_add_align_j5gqz.json`).
- Lever: `Column::aligned_binary_f64` now borrows existing contiguous Float64 buffers from `ColumnData::Float64` or lazy all-valid Float64 storage instead of rebuilding both sides with `ColumnData::from_scalars`. The same helper lets `nan_aware_validity` scan cached f64 payloads for NaN without materializing lazy scalar values; columns without a cached Float64 buffer still fall back to the previous scalar materialization path.
- Isomorphism proof: left/right position traversal stays in union-index order, missing slots still emit `Null(NaN)`, NaN source payloads still clear validity before arithmetic, valid pairs use the same `binary_f64_apply` operation and one scalar f64 operation per aligned pair, output dtype remains Float64, row/index order and duplicate/tie behavior are untouched, floating-point bits and signed zeros are copied from the same typed buffers, and no RNG state exists in this path. The new focused test verifies cloned lazy Float64 inputs stay unmaterialized while producing the same aligned output.
- Golden-output proof: `perf_profile golden series_add_align 5000` before/after sha256 stayed `0d328c9236e35ac3f85c109248a2177080d25ac7693f01873a4dc0b97d219ca2`; `tests/artifacts/perf/golden_before_series_add_align_j5gqz.txt` and `tests/artifacts/perf/golden_after_series_add_align_j5gqz.txt` compare byte-for-byte equal.
- Rebench: RCH-built candidate direct hyperfine `series_add_align 100000 100` mean 754.9 ms, stddev 26.8 ms (`tests/artifacts/perf/fp_after_series_add_align_j5gqz.json`). Paired A/B: baseline 977.9 ms +/- 34.5, candidate 743.5 ms +/- 22.0; candidate ran 1.32x +/- 0.06 faster (`tests/artifacts/perf/fp_ab_series_add_align_j5gqz.json`).
- Delta: paired result is 977.9 ms -> 743.5 ms, 24.0% faster on the profile-backed shifted Series alignment target.
- Validation: RCH `cargo test -p fp-columnar aligned_binary_f64 --lib -- --nocapture`, RCH `cargo check -p fp-columnar --all-targets`, RCH `cargo clippy -p fp-columnar --all-targets -- -D warnings`, local `cargo fmt -p fp-columnar -- --check`, `ubs crates/fp-columnar/src/lib.rs`, RCH release-perf build of `fp-conformance` example `perf_profile`, hyperfine, paired A/B, and before/after golden sha256 all passed. UBS reported the file's existing warning inventory but no critical failures.
- Score: Impact 4 x Confidence 5 / Effort 2 = 10.0, retained.
- Verdict: PRODUCTIVE

### Post-Pass 26 Shifted-Bottleneck Reprofile - 2026-06-03T13:57:00-04:00
- Artifact: `tests/artifacts/perf/fp_reprofile_after_series_align_j5gqz.json`.
- Binary: RCH-built pass-26 candidate `/data/tmp/cargo-target-rubygoose-j5gqz-current/release-perf/examples/perf_profile`.
- Hyperfine rank:
  - `join_1to1 100000 100`: 945.1 ms +/- 50.4.
  - `series_add_align 100000 100`: 805.6 ms +/- 26.0.
  - `csv_read 100000 20`: 734.0 ms +/- 10.6.
  - `filter_bool 100000 100`: 141.2 ms +/- 3.6.
  - `drop_duplicates 100000 100`: 89.8 ms +/- 11.0.
  - `sort_single 100000 100`: 50.9 ms +/- 1.6.
- Decision: pass 26 removed the typed-input reconstruction tax from aligned Float64 Series arithmetic. The next profile-backed target shifts to `join_1to1`; claim or file a `[perf]` bead for the join residual only after checking ready beads and live file reservations.

### Pass 27 - Direct Single-Key Inner Join Positions - 2026-06-03T14:29:00-04:00
- Bead claimed: `br-frankenpandas-n921e`; scope reserved for `crates/fp-join/src/lib.rs`, `crates/fp-frame/src/lib.rs`, `tests/artifacts/perf/*n921e*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: post-pass-26 reprofile ranked `join_1to1 100000 100` hottest at 945.1 ms +/- 50.4, and fresh GDB sampling hit `collect_composite_keys`, `SmallVec<[JoinKeyComponent; 1]>` construction/hash/equality, `right_map.get(key)`, and `Option<usize>` position pushes in the residual single-column inner join path (`tests/artifacts/perf/gdb_join_1to1_n921e_current.txt`).
- Baseline: RCH-built pass-26 before binary direct hyperfine `join_1to1 100000 100` mean 1.085 s, stddev 0.077 (`tests/artifacts/perf/fp_before_join_1to1_n921e.json`).
- Lever: `merge_dataframes_on_with_options` now routes unsorted single-key inner joins without indicators or validate-mode through a direct `JoinKeyComponent` hash table and `Vec<usize>` take-position output, bypassing generic per-row `SmallVec` composite keys and the later `Option<usize>` remap. Multi-key, sorted, indicator, validate, outer/left/right, and cross joins stay on the prior generic path.
- Isomorphism proof: probe order remains left-row order; per-key match order remains right-row insertion order; key equality still uses `JoinKeyComponent`; shared-key output columns, suffix handling, column order, scalar payloads, null/NaN/missing key semantics, floating-point bits, tie behavior, error classes, and RNG state are unchanged. The new focused test covers duplicate single-key inner output order.
- Golden-output proof: `perf_profile golden join_1to1 5000` before/after sha256 stayed `18988b22befdb71941112638f6f1c3415cbf0f79fc1f00ecd013318d924682c7`; `tests/artifacts/perf/golden_before_join_1to1_n921e.txt` and `tests/artifacts/perf/golden_after_join_1to1_n921e.txt` compare byte-for-byte equal.
- Rebench: RCH-built candidate direct hyperfine `join_1to1 100000 100` mean 562.4 ms, stddev 42.7 ms (`tests/artifacts/perf/fp_after_join_1to1_n921e.json`). Paired A/B: baseline 1.070 s +/- 0.061, candidate 579.6 ms +/- 46.3; candidate ran 1.85x +/- 0.18 faster (`tests/artifacts/perf/fp_ab_join_1to1_n921e.json`).
- Delta: paired result is 1.070 s -> 579.6 ms, 45.8% faster on the profile-backed shifted join target.
- Validation: RCH `cargo test -p fp-join merge_inner_single_key_preserves_duplicate_probe_order --lib -- --nocapture`, RCH `cargo check -p fp-join --all-targets`, RCH `cargo clippy -p fp-join --all-targets -- -D warnings`, local `cargo fmt -p fp-join -- --check`, `ubs crates/fp-join/src/lib.rs`, RCH release-perf build of `fp-conformance` example `perf_profile`, hyperfine, paired A/B, and before/after golden sha256 all passed. UBS reported the file's existing warning inventory but no critical failures.
- Score: Impact 4 x Confidence 5 / Effort 2 = 10.0, retained.
- Verdict: PRODUCTIVE

### Post-Pass 27 Shifted-Bottleneck Reprofile - 2026-06-03T14:29:00-04:00
- Artifact: `tests/artifacts/perf/fp_reprofile_after_join_1to1_n921e.json`.
- Binary: RCH-built pass-27 candidate `/data/tmp/cargo-target-rubygoose-n921e-candidate/release-perf/examples/perf_profile`.
- Hyperfine rank:
  - `series_add_align 100000 100`: 765.6 ms +/- 14.0.
  - `csv_read 100000 20`: 748.8 ms +/- 11.8.
  - `join_1to1 100000 100`: 550.5 ms +/- 41.2.
  - `filter_bool 100000 100`: 149.7 ms +/- 5.5.
  - `drop_duplicates 100000 100`: 82.6 ms +/- 4.6.
  - `sort_single 100000 100`: 51.9 ms +/- 5.0.
- Decision: pass 27 removed the single-key composite-key tax from inner joins. The next profile-backed target shifts back to `series_add_align`, narrowly ahead of residual CSV read; profile the residual series alignment path again before selecting the next lever.

### Pass 28 - Rejected Unit-Stride Int64 Range Alignment Path - 2026-06-03T15:13:00-04:00
- Bead claimed: `br-frankenpandas-8zgjh`; scope reserved for `crates/fp-frame/src/lib.rs`, `crates/fp-columnar/src/lib.rs`, `crates/fp-runtime/src/lib.rs`, `crates/fp-index/src/lib.rs`, `tests/artifacts/perf/*8zgjh*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: post-pass-27 reprofile ranked `series_add_align 100000 100` hottest at 765.6 ms +/- 14.0. Fresh baseline on the RCH-built pass-27 binary measured 773.2 ms +/- 28.1 (`tests/artifacts/perf/fp_before_series_add_align_8zgjh.json`), and GDB sampling hit sorted outer alignment position-map construction plus semantic witness/fingerprint frames (`tests/artifacts/perf/gdb_series_add_align_8zgjh_current.txt`).
- Attempted lever: a Float64-only fast path for duplicate-free, overlapping/touching, unit-stride Int64 index ranges that avoided generic `Option<usize>` alignment vectors by mapping labels through range offsets. A sidecar semantic review caught the disjoint-range densification trap before benchmarking, and the candidate was corrected to decline non-overlapping ranges.
- Isomorphism proof checked before rejection: row ordering stayed sorted union order for the eligible overlapping range case, duplicate/cartesian behavior fell through, mismatched names fell through to the existing name rules, alignment gaps and input NaNs materialized as `Null(NaN)`, floating-point operations used the same `binary_f64_apply`, and RNG state was not involved. Golden output for `series_add_align 5000` stayed byte-identical with sha256 `0d328c9236e35ac3f85c109248a2177080d25ac7693f01873a4dc0b97d219ca2`.
- Validation before rejection: RCH focused tests for the range helper, disjoint fallback, and Series Float64 overlap path passed; RCH `cargo check -p fp-columnar -p fp-frame --all-targets`, RCH `cargo clippy -p fp-columnar -p fp-frame --all-targets -- -D warnings`, local `cargo fmt -p fp-columnar -p fp-frame -- --check`, RCH release-perf `perf_profile` build, hyperfine, paired A/B, and before/after golden sha256 all completed. UBS on `crates/fp-columnar/src/lib.rs crates/fp-frame/src/lib.rs` hung in its Rust module and was terminated as a tooling timeout; no UBS finding was produced.
- Rebench: candidate direct hyperfine regressed to 837.0 ms +/- 38.9 (`tests/artifacts/perf/fp_after_series_add_align_8zgjh.json`). Paired A/B confirmed old 777.8 ms +/- 22.8 vs candidate 804.2 ms +/- 17.4, with old 1.03x +/- 0.04 faster (`tests/artifacts/perf/fp_ab_series_add_align_8zgjh.json`).
- Decision: Score < 2.0; the code lever was removed and not retained. The next Series pass must attack a different structural primitive, not this dense-range offset path: profile whether compressed/lazy alignment plans, index-fingerprint/witness cost elimination, or a run-segment alignment representation gives a target ratio >=1.15 before implementing.
- Verdict: REJECTED

### Pass 29 - Rejected Sorted-Unique Streamed Float64 Alignment - 2026-06-03T15:51:00-04:00
- Bead claimed: `br-frankenpandas-m7b5d`; scope reserved for `crates/fp-frame/src/lib.rs`, `crates/fp-columnar/src/lib.rs`, `crates/fp-index/src/lib.rs`, `crates/fp-runtime/src/lib.rs`, `tests/artifacts/perf/*m7b5d*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: post-pass-27 and pass-28 evidence kept `series_add_align 100000 100` as the hottest residual Series target. Fresh baseline on the RCH-built pass-27 binary measured 779.5 ms +/- 18.0 (`tests/artifacts/perf/fp_before_series_add_align_m7b5d.json`), and fresh GDB sampling again hit `align_union_merge_sorted` position-vector pushes plus semantic witness/fingerprint SHA256 frames (`tests/artifacts/perf/gdb_series_add_align_m7b5d_current.txt`).
- Attempted lever: a sorted-unique Float64 streaming path that built the output union index for the existing witness/policy boundary, then streamed aligned arithmetic directly from sorted label cursors after admission to avoid materializing the two `Vec<Option<usize>>` position maps. Duplicate, unsorted, exact-index, and non-Float64 cases stayed on the existing materialized `AlignmentPlan`.
- Isomorphism proof checked before rejection: sorted union label ordering matched `align_union_sorted_unique`; exact/duplicate/unsorted paths fell through; output index names followed the same shared-name rule; alignment gaps and input NaNs materialized as `Null(NaN)`; floating-point operations used the same `binary_f64_apply`; witness recording and runtime policy admission stayed before value materialization; RNG state was not involved. Golden output for `series_add_align 5000` stayed byte-identical with sha256 `0d328c9236e35ac3f85c109248a2177080d25ac7693f01873a4dc0b97d219ca2`.
- Validation before rejection: RCH `cargo test -p fp-columnar aligned_binary_f64_sorted_unique_matches_position_plan --lib -- --nocapture`, RCH `cargo test -p fp-frame sorted_unique_stream --lib -- --nocapture`, RCH `cargo check -p fp-columnar -p fp-frame --all-targets`, RCH `cargo clippy -p fp-columnar -p fp-frame --all-targets -- -D warnings`, local `cargo fmt -p fp-columnar -p fp-frame -- --check`, RCH release-perf `perf_profile` build, hyperfine, paired A/B, and before/after golden sha256 all completed. UBS on `crates/fp-columnar/src/lib.rs crates/fp-frame/src/lib.rs` timed out after 300 seconds in the Rust scanner without emitting a finding.
- Rebench: candidate direct hyperfine regressed to 810.1 ms +/- 27.9 (`tests/artifacts/perf/fp_after_series_add_align_m7b5d.json`). Paired A/B confirmed old 767.0 ms +/- 16.6 vs candidate 789.0 ms +/- 13.8, with old 1.03x +/- 0.03 faster (`tests/artifacts/perf/fp_ab_series_add_align_m7b5d.json`).
- Decision: Score < 2.0; the code lever was removed and not retained. The next Series pass should stop attacking position maps first and instead attack the other repeatedly sampled structural cost: semantic witness/index fingerprint reuse, cached output index identity, or a deeper compressed witness primitive with a target ratio >=1.15.
- Verdict: REJECTED

### Pass 30 - Cached Sorted-Unique Alignment Witness Fingerprints - 2026-06-03T17:15:56-04:00
- Bead claimed: `br-frankenpandas-eb1pb`; scope reserved by bead ownership for `crates/fp-frame/src/lib.rs`, `tests/artifacts/perf/*series_add_align_eb1pb*`, `.skill-loop-progress.md`, and `.beads/issues.jsonl`.
- Profile-backed target: pass-27 shifted reprofile ranked `series_add_align 100000 100` hottest at 765.6 ms +/- 14.0, and the rejected pass-28/pass-29 GDB evidence repeatedly sampled the residual `record_alignment_semantic_witness -> semantic_index_identity -> semantic_integer_index_labels_fingerprint -> SHA256/update_i64_decimal` chain after position-map levers failed their retain score.
- Alien primitive: semantic witness memoization from the compressed-witness/reuse family. Instead of micro-tuning SHA256 decimal emission, reuse the deterministic output-index semantic fingerprint for sorted, duplicate-free left/right index pairs once the already-computed left/right fingerprints identify the same sorted-union witness boundary.
- Baseline: RCH-built pass-27 before binary paired hyperfine `series_add_align 100000 100` mean 772.3 ms, stddev 38.4 ms (`tests/artifacts/perf/fp_ab_series_add_align_eb1pb.json`; stored direct before artifact `tests/artifacts/perf/fp_before_series_add_align_eb1pb.json` mean 747.9 ms).
- Lever: `record_alignment_semantic_witness` now computes left/right semantic identities once and asks a bounded sorted-unique union fingerprint cache for the output identity. The cache admits only sorted, duplicate-free indexes and revalidates the passed left/right fingerprints against the actual indexes before keying the cache, so duplicate-aware, unsorted, and mismatched private-helper calls fall back to exact output fingerprinting.
- Isomorphism proof: left/right/output witness roles, lengths, dtype names, index ordering, tie-breaking, duplicate semantics, missing-slot materialization, floating-point operations, null/NaN propagation, and RNG state are unchanged. Exact witness bytes are preserved because cache hits return only a previously computed output-index semantic fingerprint for the same left/right label fingerprints and lengths; unsorted or duplicate indexes still compute the output fingerprint directly. Focused tests cover cache hit identity equivalence, mismatched-fingerprint rejection, duplicate/unsorted fallback identity, eviction behavior, and concurrent cache access.
- Golden-output proof: `perf_profile golden series_add_align 5000` before/after sha256 stayed `0d328c9236e35ac3f85c109248a2177080d25ac7693f01873a4dc0b97d219ca2`; `tests/artifacts/perf/golden_before_series_add_align_eb1pb.txt` and `tests/artifacts/perf/golden_after_series_add_align_eb1pb.txt` compare byte-for-byte equal.
- Rebench: RCH-built candidate paired A/B hyperfine `series_add_align 100000 100` measured old 772.3 ms +/- 38.4 vs candidate 471.5 ms +/- 21.5; candidate ran 1.64x +/- 0.11 faster (`tests/artifacts/perf/fp_ab_series_add_align_eb1pb.json`).
- Delta: paired result is 772.3 ms -> 471.5 ms, 39.0% faster on the profile-backed Series alignment semantic-witness target.
- Validation: RCH `cargo test -p fp-frame sorted_unique_union_fingerprint --lib -- --nocapture`, RCH `cargo test -p fp-frame alignment_semantic_witness --lib -- --nocapture`, RCH `cargo check -p fp-frame --all-targets`, RCH `cargo clippy -p fp-frame --all-targets -- -D warnings`, local `cargo fmt -p fp-frame -- --check`, RCH release-perf build of `fp-conformance` example `perf_profile`, hyperfine paired A/B, shifted-bottleneck hyperfine reprofile, and before/after golden sha256 all passed. UBS on `crates/fp-frame/src/lib.rs` timed out after 180 seconds in the Rust scanner without emitting a finding.
- Score: Impact 4 x Confidence 5 / Effort 2 = 10.0, retained.
- Verdict: PRODUCTIVE

### Post-Pass 30 Shifted-Bottleneck Reprofile - 2026-06-03T17:15:56-04:00
- Artifact: `tests/artifacts/perf/fp_reprofile_after_series_witness_eb1pb.json`.
- Binary: RCH-built pass-30 candidate `/data/tmp/cargo-target-blackthrush-eb1pb-final/release-perf/examples/perf_profile`.
- Hyperfine rank:
  - `csv_read 100000 20`: 698.4 ms +/- 9.7.
  - `join_1to1 100000 100`: 604.3 ms +/- 30.8.
  - `series_add_align 100000 100`: 471.3 ms +/- 12.8.
  - `filter_bool 100000 100`: 147.5 ms +/- 8.1.
  - `drop_duplicates 100000 100`: 83.0 ms +/- 5.9.
  - `sort_single 100000 100`: 53.1 ms +/- 5.5.
- Decision: pass 30 removed the residual sorted-union semantic-witness fingerprinting tax from aligned Series arithmetic. The next profile-backed target shifts to `csv_read`; attack a deeper parser primitive such as zero-copy token classification, batched numeric-column filling, or arena-backed row framing rather than another Series alignment micro-lever.
